# OpenLife PromptStack Coverage Audit

Date: 2026-05-25

Status: PromptStack full-path audit and safety-net matrix. This is a code-fact document, not a migration plan completion claim.

Scope: Chat, StreamChat, Scheduled, PlanMode / Plan execution, Replay, Skill runtime, Builder, Calibration, Proactive suggestions, Direct tool execution, plus additional prompt assembly findings discovered by source audit.

## Coverage Matrix

| Entrypoint | Prompt source | PromptStack-governed | AgentSpec source | Event trace emitted | Privacy behavior | Remaining risk | Next required migration |
|---|---|---|---|---|---|---|---|
| Chat | `send_message_with_agent_loop_inner` builds a Tauri `PromptBlockRegistry`, enters `run_tauri_agent_task(Chat)`, and `AgentRuntime::execute_task_with_spec` assembles AgentSpec prompt blocks before `InferenceScheduler::generate_governed`. Runtime fallback may use legacy direct generation only after the facade returns a Runtime error. | Yes for formal AgentLoop call. Unknown PromptBlock fails as a Runtime error. | Required stored/default `AgentSpec` resolved before facade call and matched against `ActionContext.agent_spec`. | `run.created`, `agent_spec.selected`, `prompt_stack.assembled`, `context_governance.applied`, model/tool events, `run.completed` or `run.failed` when the run is created. Runtime fallback remains separately traceable by existing fallback events. PromptStack assembled event payload is metadata-only and excludes raw prompt. Context governance event payload is metadata-only and excludes raw LifeModel. | ContextAssembler desensitizes messages; `generate_governed` preserves existing PromptStack system message and applies LocalOnly / SummaryOnly / CloudAllowed policy. | Runtime fallback branch can still use legacy generation after a governed runtime failure; it must not be used for Governance failures. | Keep facade/source tests locked; next phase should decide whether Runtime fallback should become a first-class governed fallback spec instead of legacy direct generation. |
| StreamChat | `start_stream_message_with_agent_loop` builds a Tauri `PromptBlockRegistry`, enters `run_tauri_agent_task(StreamChat)`, and `AgentRuntime::execute_task_with_spec` assembles AgentSpec prompt blocks before `generate_stream_governed`. | Yes for formal streaming AgentLoop call. Unknown PromptBlock fails closed before chunks are emitted by the facade path. | Required stored/default `AgentSpec` resolved before facade call and matched against `ActionContext.agent_spec`. | Same AgentLoop governance events as Chat when a run is created. Stream command also emits UI stream events; Runtime fallback remains caller-owned. PromptStack assembled event payload is metadata-only and excludes raw prompt. Context governance event payload is metadata-only and excludes raw LifeModel. | Same as Chat, with streaming holdback before visible chunks. | Runtime fallback branch still exists for model/runtime failures; Governance errors emit stream error only. | Keep new unknown-PromptBlock StreamChat behavior test; later migrate Runtime fallback into an explicit governed fallback contract. |
| Scheduled | `execute_scheduled_task` resolves a required `AgentSpec`, calls `build_prompt_registry`, then enters `run_tauri_scheduled_execution`; internal AgentLoop uses `execute_task_with_spec` and `generate_governed`. | Yes for scheduled model execution. Unknown PromptBlock produces a failed scheduled AgentRun when run creation has occurred, and no Chat fallback. | Stored default `AgentSpec` resolved by scheduler runner before scheduled wrapper; missing spec fails before run creation. | Successful and runtime-failed runs emit normal AgentLoop events, including `prompt_stack.assembled` and `context_governance.applied` when runtime reaches governance. Scheduler task status also records `agent_run_id` only when available. PromptStack assembled event payload is metadata-only and excludes raw prompt. Context governance event payload is metadata-only and excludes raw LifeModel. | Scheduled wrapper uses planner role, restricted toolset, governed `NetworkPolicy`, `ExecutionSandbox`, and model privacy policy from AgentSpec. | Missing AgentSpec has no AgentRunEvent because no run exists; scheduler task failure/status is the only trace. | Keep scheduled wrapper locked; do not migrate Builder/Calibration/Skill through Scheduled. |
| PlanMode / Plan execution | `PlanModeRunner::build_planning_prompt_stack` can build a planning `PromptStack`; Tauri `execute_agent_plan` / `retry_agent_plan` are action execution flows through `run_tauri_plan_execution` and core `PlanExecutor`, not model prompt assembly. | Mixed. PlanMode prompt helper is PromptStack-based; Tauri plan execution itself has no model PromptStack and should not emit a fake PromptStack trace. | Plan-bound `AgentSpec`, or stored default for unbound plans, resolved fail-closed by plan wrapper before execution/retry mutation. | Plan lifecycle events and tool/action events. No `prompt_stack.assembled` for direct plan-step execution unless a plan was originally created by an AgentLoop path. | Governed `ActionContext` with `NetworkPolicy`, `ExecutionSandbox`, ToolPermission store, and AgentSpec tool allowlist. | Planning prompt helper and plan execution are easy to conflate in docs; current action execution has no prompt trace because it has no model prompt. | Keep Plan-specific wrapper; if plan generation/model planning is exposed as a Tauri entrypoint, route that model call through AgentRuntime/PromptStack and trace it separately. |
| Replay | Replay restores original run/action and executes the original action through `run_tauri_replay_execution`; there is no model prompt. | Not applicable. Replay should not use PromptStack because it replays an already planned action. | Original run `agent_spec_id` or plan-bound spec, restored fail-closed before action execution. | `replay.started`, `replay.completed`, `replay.failed`, plus tool/action block details. No PromptStack trace should be emitted. | Uses current governed `ActionContext`, `NetworkPolicy`, `ExecutionSandbox`, ToolPermission checks, and original action metadata. | Replay may be triggered from Proposal acceptance, so Proposal status must remain the source of truth. | Keep Replay-specific wrapper; do not move replay into Chat facade or add fake prompt events. |
| Skill runtime | Skill-specific PromptStack contract blocks derived from SkillManifest and `skill_id` are registered into a Skill-only `PromptBlockRegistry`; raw user input is carried as a normal user message, not as a Skill system block. A cloned effective `AgentSpec` appends those Skill contract block IDs and still executes through `AgentRuntime::execute_task_with_spec` before `generate_governed`. `SkillRegistry::build_system_prompt` / `build_skill_prompt` remain only legacy/parsing compatibility helpers, not the formal runtime prompt path. | Yes for Skill-specific prompt. AgentSpec PromptStack, Skill contract PromptBlocks, and context governance are active. Unknown PromptBlock persists a failed Skill AgentRun with metadata-only `run.failed`. | Required stored/default `AgentSpec`; missing spec fails before run/model/fallback. SkillManifest `allowed_tools` is checked against AgentSpec before Skill PromptStack assembly. | `agent_spec.selected`; on success, `prompt_stack.assembled` includes AgentSpec and Skill PromptBlock metadata after run persistence, then `context_governance.applied`; on PromptStack/model failure, `run.failed` is emitted with bounded readable error only. PromptStack assembled event payload is metadata-only and excludes raw prompt. Context governance event payload is metadata-only and excludes raw LifeModel. | `generate_governed` privacy policy from AgentSpec; failure payloads exclude raw skill prompt, raw LifeModel, sensitive context, and full model output. SummaryOnly cloud keeps non-sensitive Skill contract blocks (`skill_id`, name/purpose, allowed tools contract, input/output schema, required JSON envelope, proposal policy, pure-JSON/no-markdown constraints) while filtering raw user input, raw LifeModel, raw memory, recent runs, and chat history. SummaryOnly does not parse user-controlled text markers for Skill block boundaries; prompt-boundary / marker injection is covered by tests. SkillContext event trace records only available context categories, not raw context. | Skill runtime remains outside Chat ExecutionFacade and still owns model generation, JSON envelope parsing, proposal writes, and failure semantics in `run_skill_with_state`. Builder and Calibration are intentionally unchanged. | Keep Skill-specific PromptStack boundary locked; later decide whether the remaining parse/proposal lifecycle should become a Skill-specific facade, without reusing Chat facade. |
| Chat proposal extraction | `ChatProposalGenerator::try_llm_extract` now assembles Proposal-specific PromptBlocks (`proposal_extraction.role`, `proposal_extraction.output_contract`, `proposal_extraction.privacy_rules`) plus a privacy-scoped task input block before local extraction. The old direct ad hoc extraction prompt was removed. | Yes for the proposal extraction prompt helper. Unknown PromptBlock and malformed PromptStack return structured failure reasons and enter explicit heuristic fallback. This path is not routed through Chat ExecutionFacade. | No stored AgentSpec yet; this phase uses a dedicated Proposal Extraction PromptStack helper and stable block IDs/versions. | No AgentRunEvent store is available at this layer today, so it does not emit fake `prompt_stack.assembled`. It returns structured audit metadata: block IDs/versions/purpose, privacy policy, local route, model failure reason, fallback reason, and cloud-attempt flag. Audit metadata excludes raw prompt, raw LifeModel, raw user message, and full model output. | LocalOnly uses only local Ollama or explicit heuristic fallback; it never silently routes to cloud. SummaryOnly task input includes only contract/counts/signal hints and omits raw user text and raw LifeModel. CloudAllowed may include user task input for the model but still keeps audit metadata metadata-only. | Event integration remains missing because chat proposal extraction currently has no AgentRunEvent store boundary. Model route is local-only; cloud extraction is not implemented. | Later add Proposal Extraction AgentSpec/EventStore integration if this path becomes a first-class AgentRun, without moving it into Chat ExecutionFacade. |
| Web content summarization helper | `openlife-core/src/agent/action_executor/helpers.rs::summarize_content_blocking` now assembles Web Summarization PromptBlocks (`web_summarization.role`, `web_summarization.output_contract`, `web_summarization.privacy_rules`, `web_summarization.task_input`) before local summarization. The old raw summarizer system prompt was removed. | Yes for the helper prompt. Unknown PromptBlock fails closed with `unknown_prompt_block` / `prompt_stack_assembly_failed`; validation failure, local model unavailable/timeout, and invalid model output all return stable metadata-only failure reasons and explicit fallback output. This path is not routed through Chat ExecutionFacade. | No stored AgentSpec yet; this phase uses a dedicated Web Summarization PromptStack helper and stable block IDs/versions. | No AgentRunEvent store is available at this helper layer, so it does not emit fake `prompt_stack.assembled`. It returns/internalizes `WebSummarizationAudit`: block trace, privacy policy, local route/provider, failure/fallback reason, source type, and content length only. | LocalOnly uses only local Ollama or explicit non-raw fallback; it never routes to cloud. SummaryOnly task input omits raw web content and raw URL, retaining only length/source type/contract/statistics and returns an explicit omitted-content fallback. Audit metadata, failure fallback, and user-visible successful helper output/tool observation must not include raw URL query values, fragments, userinfo, signed URL params, raw prompt/content, or full model output; successful output uses a sanitized source display containing only scheme/host/path, and invalid sources become `redacted_source`. CloudAllowed is still not implemented as a cloud route; if enabled later, audit and user-visible output must remain sanitized. | Event integration remains missing because web summarization has no AgentRunEvent store boundary. The helper remains under ActionExecutor web.fetch, not Chat ExecutionFacade, and cloud summarization is not implemented. | Later decide whether web summarization should become an AgentSpec/EventStore-backed AgentRun helper, without adding fake prompt events or moving it into Chat facade. |
| LayeredReasoner internal prompts | `openlife-core/src/agent/reasoning/layered.rs` now assembles meaning / strategy / generation / safety role and contract text from LayeredReasoner PromptBlocks (`layered_reasoner.*`) and privacy-scoped task/constraint blocks. The old strategy/generation naked prompt strings were removed from the runtime path. | Yes as a PromptStack-governed internal strategy boundary. Unknown internal PromptBlock fails closed with `unknown_prompt_block`; validation warnings fail with `prompt_stack_validation_failed`. AgentSpec PromptStack remains the outer runtime boundary at message index 0. | The outer AgentRuntime still uses the stored/default `AgentSpec`. LayeredReasoner internal PromptBlocks come from the built-in registry, not from AgentSpec mutation. | No new AgentRunEvent is emitted for each internal phase. The phase trace is metadata-only through `ReasoningTrace.prompt_block_traces` and safety result `prompt_blocks`; it does not emit fake `prompt_stack.assembled` and does not store raw assembled prompt. | SummaryOnly meaning/strategy/generation task prompts omit raw user text, raw LifeModel, goal names/descriptions, raw memory, and raw tools text, keeping only counts and non-identifying signals. LocalOnly still uses `generate_raw_governed` and fails if a local model is unavailable. CloudAllowed may include governed task input for model execution, but trace metadata remains block id/version/purpose only. | Internal phase traces are not first-class AgentRunEvent records yet; they remain inside `ReasoningTrace`. This is intentional until an event-store boundary is designed for internal reasoning phases. | Keep metadata-only internal boundary locked. If phase-level events are added later, emit a distinct event type rather than faking `prompt_stack.assembled`. |
| Builder | `BuilderEngine` scripted/quick/Socratic prompts and deterministic signal extraction; Tauri Builder commands create proposals or gated legacy direct apply, not AgentRuntime model calls. Builder prompt remains legacy/ad hoc. | No. Intentionally legacy/not complete; current safety net is Proposal-first, not PromptStack completion. | No formal AgentSpec for Builder commands today. | Builder proposal path emits metadata-only `proposal.created` and Builder AgentRun records. No `prompt_stack.assembled` trace. | Proposal-created payload excludes raw prompt, before/after values, and full LifeModel; direct apply is default-disabled behind `system.allow_legacy_builder_direct_apply`. | Builder prompts and session draft semantics are outside PromptStack; legacy direct apply path remains for explicit dev/test compatibility. | Migrate Builder prompt generation through a Builder-specific governed AgentSpec/PromptStack boundary after LifeModel Evolution proposal semantics are finalized. |
| Calibration | Feedback report and MicroEvolution paths are deterministic/data-derived; Calibration proposal creation writes Review Center proposals. Calibration prompt remains legacy/ad hoc for user-facing calibration prompt metadata and has no AgentRuntime PromptStack boundary. | No. Intentionally legacy/not complete; Proposal-first safety does not imply PromptStack governance. | No formal AgentSpec for Calibration commands today. | Calibration proposal path emits metadata-only `proposal.created`; direct apply creates calibration AgentRun only when explicit legacy gate is enabled. No `prompt_stack.assembled` trace. | Proposal-created payload excludes raw prompt, before/after values, and full LifeModel; direct apply is default-disabled behind `system.allow_legacy_calibration_direct_apply`. | No PromptStack boundary for calibration report/evolution or UI prompt metadata; direct legacy path remains dev/test gated. | Define Calibration-specific AgentSpec/PromptStack only if calibration becomes a model-generation path; otherwise document it as deterministic/proposal-only. |
| Proactive suggestions | `ProactiveEngine::generate_suggestions` builds suggestion prompt strings from LifeModel/proposal counts and returns them to the UI/scheduler. | Not applicable for suggestion-only generation. Proactive suggestions are suggestion-only and do not create AgentRun or PromptStack trace. | None at suggestion generation time. Accepted suggestions that become scheduled tasks use the Scheduled AgentSpec path. | No AgentRun and no AgentRunEvent for suggestion generation. Accepted scheduled execution later emits Scheduled traces. | Suggestion generation reads LifeModel locally; no model call is made in this path. | Suggestion prompt strings can contain LifeModel-derived text, so they must not be mistaken for model payloads or traced as prompt_stack events. | Keep suggestion-only source audit locked; if proactive auto-execution is added, enter through Scheduled-specific wrapper. |
| Direct tool execution | `execute_tool_call_inner` builds governed Tauri action context and calls `run_tauri_direct_tool_execution`; there is no model prompt. | Not applicable. Direct tool execution should not use PromptStack because no model prompt is assembled. | Required stored/default `AgentSpec`, matched through governed `ActionContext`. | Direct tool audit run and tool/action outcomes; `tool.call_blocked` when governance blocks. No PromptStack trace should be emitted. | ToolPermission, `NetworkPolicy`, `ExecutionSandbox`, and AgentSpec tool allowlist govern execution. | Direct tool results can be confused with AgentLoop tool calls; it must remain a no-model action path. | Keep direct-tool facade wrapper locked; do not add ad hoc model planning to this command. |

## Additional Legacy Prompt Findings

| Finding | Prompt source | Current status | Required next step |
|---|---|---|---|
| Legacy scheduler generation | `InferenceScheduler::generate` / `generate_stream` and `llm::build_system_prompt` can still inject LifeModel PromptStack-style system text for non-governed callers. | Legacy compatibility path. It uses `PromptStack::chat_system_stack` text but has no AgentSpec source and no `prompt_stack.assembled` AgentRunEvent. | Reduce callers over time; keep `generate_governed` as the formal path for Agent execution. |

## Safety-Net Tests

- `execution_facade_chat_uses_prompt_registry`: Chat unknown PromptBlock fails through PromptStack instead of silently using an ad hoc prompt.
- `execution_facade_stream_chat_unknown_prompt_block_fails_closed`: StreamChat unknown PromptBlock fails closed before successful streaming.
- `scheduled_facade_prompt_stack_runtime_error_does_not_fallback`: Scheduled unknown PromptBlock is observable as a runtime failure and does not use Chat fallback.
- `skill_specific_prompt_stack_contains_manifest_contract_and_metadata`: Skill PromptStack includes skill id, purpose, allowed tools, input schema, and output JSON envelope requirements while trace metadata excludes raw prompt/context.
- `test_summary_only_skill_promptstack_preserves_contract_and_filters_raw_input`: SummaryOnly cloud payload preserves Skill IO contract / JSON envelope / proposal policy while removing raw skill user input and raw context sentinels.
- `test_summary_only_skill_promptstack_marker_injection_does_not_leak_raw_input`: User-controlled fake Skill block markers cannot influence SummaryOnly contract preservation or leak raw input.
- `skill_runtime_prompt_stack_failure_persists_failed_run_with_safe_payload`: Unknown PromptBlock creates an observable failed Skill run and safe `run.failed` payload.
- `proposal_extraction_prompt_stack_has_stable_contract_blocks`: Proposal extraction has stable block IDs/versions and includes supported proposal types plus JSON output contract.
- `test_model_json_parse_failure_has_explicit_heuristic_fallback_reason`: invalid model JSON enters explicit heuristic fallback with metadata-only failure reason.
- `test_unknown_proposal_prompt_block_falls_back_with_audit_metadata`: unknown Proposal PromptBlock does not silently proceed and records `prompt_stack_assembly_failed`.
- `proposal_extraction_summary_only_prompt_excludes_raw_sentinels` / `test_summary_only_audit_metadata_excludes_raw_prompt_model_output_and_sentinels`: SummaryOnly proposal extraction excludes raw user/LifeModel/model-output sentinels from prompt/audit boundaries.
- `test_local_only_unavailable_model_does_not_use_cloud`: LocalOnly local-model failure records `local_model_unavailable`, uses heuristic fallback, and does not attempt cloud.
- `web_summarization_prompt_stack_has_stable_contract_blocks`: Web summarization has stable block IDs/versions/purpose and JSON output contract.
- `web_summarization_summary_only_prompt_excludes_raw_sentinels` / `test_summary_only_web_summarization_audit_metadata_excludes_raw_content`: SummaryOnly web summarization excludes raw fetched content and raw URL sentinels from assembled prompt and audit metadata.
- `test_local_only_web_summarization_unavailable_does_not_use_cloud`: LocalOnly web summarization records `local_model_unavailable`, does not attempt cloud, and returns a non-raw fallback.
- `test_unknown_web_summarization_prompt_block_fails_closed` / `test_invalid_web_summarization_model_output_has_stable_failure_reason`: Web summarization fail-closes on unknown blocks and invalid model output without falling back to a bare prompt or raw content.
- `layered_reasoner_prompt_stack_has_stable_contract_blocks`: LayeredReasoner meaning / strategy / generation / safety / privacy blocks have stable IDs, versions, purposes, and output contracts.
- `test_unknown_layered_reasoner_prompt_block_fails_closed`: unknown internal LayeredReasoner blocks fail closed with `unknown_prompt_block` and do not continue through a legacy naked prompt.
- `layered_reasoner_summary_only_prompt_excludes_raw_sentinels` / `test_layered_reasoner_block_trace_excludes_raw_prompt_and_sentinels`: SummaryOnly internal prompts and block-trace metadata exclude raw user, raw LifeModel, goal, memory, and prompt sentinels.
- `test_prompt_stack_assembled_payload_contract` and `test_context_governance_applied_payload_contract`: typed payload builders exclude raw prompt and raw LifeModel.
- `prompt_stack_source_audit_classifies_governed_legacy_and_not_applicable_paths`: source/document lock for governed, intentionally legacy, and not-applicable paths.

## Current Gaps

Builder prompt remains legacy/ad hoc. Calibration prompt remains legacy/ad hoc. Skill runtime remains outside Chat ExecutionFacade. Proactive suggestions are suggestion-only and do not create AgentRun or PromptStack trace. Chat proposal extraction and web summarization now have dedicated PromptStack helpers and structured metadata-only audit, but still have no AgentRunEvent store integration. LayeredReasoner internal prompts are now a PromptStack-governed internal strategy boundary with metadata-only `ReasoningTrace` block traces and no fake `prompt_stack.assembled` events. Legacy scheduler generation remains a legacy compatibility prompt path. PromptStack assembled event payload is metadata-only and excludes raw prompt. Context governance event payload is metadata-only and excludes raw LifeModel.
