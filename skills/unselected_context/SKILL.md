# Unselected Context

This skill exists to prove that unselected `SKILL.md` files are not injected into
Main Chat context.

It should never appear in a task context unless explicitly selected by stable
skill id.
