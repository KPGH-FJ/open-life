# Unselected Sensitive Skill

This file exists only as negative Stage 1 evidence.

It must not be injected into Main Chat context unless explicitly selected by
stable skill id. It must not override policy, privacy, or tool permissions.
