# OpenLife 产品需求文档 (PRD) - 最终版

> **文档版本**: v3.0
> **生成日期**: 2025年
> **文档状态**: 历史参考
> **适用范围**: 长期愿景与历史需求参考，不作为当前 Agent 开发顺序依据
>
> 2026-05-30 说明：当前 Agent 开发入口以 `AGENTS.md`、`plans/README.md`
> 和 `plans/openlife_lifemodel_governed_agent_runtime.md` 为准。本 PRD 不得
> 覆盖当前 LifeModel-Governed Runtime 开发顺序或 Tool Taxonomy。

---

## 目录

0. [执行收敛说明（2026-04）](#0-执行收敛说明2026-04)
1. [文档信息](#1-文档信息)
2. [产品概述](#2-产品概述)
3. [市场分析](#3-市场分析)
4. [功能需求](#4-功能需求)
5. [技术架构](#5-技术架构)
6. [实现路线图](#6-实现路线图)
7. [成功指标](#7-成功指标)
8. [风险与缓解](#8-风险与缓解)

---

## 0. 执行收敛说明（2026-04）

> 本节用于把 PRD 的长期目标态与当前代码基线对齐。
> 它不替代原有愿景，而是规定 **当前版本真正执行的范围**。

### 0.1 当前判断

OpenLife 当前不再是概念验证项目，而是一个已经具备可运行主链路的 Alpha 阶段桌面应用：

- 已有可运行骨架：`Rust core + Tauri shell + React UI`
- 已跑通主链路：对话、会话管理、基础人生模型、隐私脱敏、MCP/A2A、版本快照、向量记忆、Builder 入口
- 当前主要问题不是“缺功能”，而是“功能不少，但系统边界、契约一致性和工程收口还不足”

因此，本 PRD 在保留长期愿景的同时，当前执行策略从“全面铺开”调整为：

1. 先把核心链路做稳
2. 再把人生模型和 Builder 做透
3. 然后做记忆、校准、进化闭环
4. 最后再扩展生态与长期叙事

### 0.2 当前版本的第一优先级

当前版本（v1 Alpha 收口阶段）的最高优先级不是新增概念，而是完成以下四个可验证闭环：

1. 对话闭环：输入 -> 路由 -> 隐私处理 -> 模型调度 -> 工具调用 -> 回复 -> 记忆落库
2. 人生模型闭环：Builder/Editor -> 结构化模型 -> 仪表盘/对话消费
3. 校准闭环：反馈/行为 -> 变更建议 -> 用户确认 -> 快照/回滚
4. 工具闭环：发现 -> 风险分级 -> 待确认 -> 执行 -> 审计

### 0.3 当前阶段明确暂缓的方向

以下方向继续保留在 PRD 中作为中长期目标，但 **不进入当前阶段主计划**：

- 数字遗产/法律合规继承
- 联邦 Agent 网络
- 多平台同步
- 插件市场和社区分享
- 极致性能目标（例如 Layer 1 的 `<50ms P99`）在没有稳定基线前不作为当前阻塞验收条件

### 0.4 当前阶段的范围边界

当前阶段只承诺以下结果：

- 让 OpenLife 成为“稳定、可持续迭代的本地人生对话系统”
- 让人生模型真正驱动建议、规划、校准和工具调用
- 让高风险工具调用具备可审计、可确认、可回滚的安全边界

不承诺以下结果：

- 一步到位成为完整的个人操作系统
- 一步到位成为成熟的 Agent 平台
- 一步到位解决所有长期人格/继承/代理社交问题

### 0.5 执行文档约束

从本次维护开始，PRD 负责回答“为什么做、最终要到哪”，实际开发执行统一以以下文档为准：

- 主计划：[plans/openlife_development_plan.md](/Users/fujing/Desktop/偶来福/plans/openlife_development_plan.md)
- Codex 执行手册：[plans/openlife_codex_execution_playbook.md](/Users/fujing/Desktop/偶来福/plans/openlife_codex_execution_playbook.md)

---

## 1. 文档信息

### 1.1 文档版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v1.0 | 2025-01 | 产品团队 | 初始PRD文档 |
| v2.0 | 2025-02 | 架构团队 | 技术架构补充 |
| v3.0 | 2025年 | 整合团队 | 完整开发版PRD |

### 1.2 文档目标读者

| 角色 | 关注重点 |
|------|----------|
| **产品经理** | 功能需求、用户故事、验收标准 |
| **架构师/技术负责人** | 系统架构、接口定义、数据模型 |
| **开发工程师** | API规范、代码示例、实现细节 |
| **测试工程师** | 测试策略、验收标准、性能指标 |
| **项目经理** | 路线图、里程碑、依赖关系 |

### 1.3 相关文档

- 原始PRD: `/mnt/okcomputer/upload/OpenLife_Complete_PRD.md`
- 技术架构: `/mnt/okcomputer/upload/OpenLife_Technical_Architecture_PRD.md`
- 市场分析: `/mnt/okcomputer/output/openlife_market_analysis_report.md`
- 产品分析: `/mnt/okcomputer/output/OpenLife_PRD_Analysis.md`

---

## 2. 产品概述

### 2.1 一句话定位

**OpenLife 是一个完全开源的本地人生模型框架，作为用户的终身成长合伙人，基于用户构建的人生模型自主推进中长期目标，在分级授权范围内执行操作、代理社交互动，并支持模型的全生命周期管理与继承。**

### 2.2 核心价值主张

| 维度 | 传统AI助手 | Hermes/OpenClaw | **OpenLife** |
|------|-----------|-----------------|--------------|
| **关系定位** | 工具/助手 | 个人代理 | **人生合伙人** |
| **核心资产** | 聊天记录 | 技能+偏好 | **人生模型(四维)** |
| **数据存储** | 云端 | 本地 | **本地加密+可继承** |
| **进化目标** | 对话质量 | 任务效率 | **人生意义对齐** |
| **隐私保护** | ❌ 上传原始数据 | ✅ 本地处理 | **✅ 脱敏上云** |
| **长期视角** | 会话级 | 项目级 | **终身级** |
| **用户主权** | 平台所有 | 用户所有 | **用户完全所有+继承** |

### 2.3 设计哲学

```
原则 1: 人生模型是数据，不是权重
─────────────────────────────────
❌ 错误理解: 通过微调LLM权重来"记住"用户
✅ 正确理解: 结构化数据文件 (YAML + SQLite)，LLM读取配置

原则 2: 双轴分层架构
────────────────────
纵向 (能力): Layer 1 → Layer 2 → Layer 3 (由快到慢)
横向 (记忆): Tier 0 → Tier 1 → Tier 2 → Tier 3 → Tier 4 (由热到冷)

原则 3: 渐进式进化
──────────────────
微进化 (自动) → 周期校准 (轻量确认) → 深度进化 (深度参与)

原则 4: 多源数据融合
──────────────────
对话记忆 (权重0.2) + 行为数据 (权重0.3) + 显式反馈 (权重0.5)

原则 5: 用户最终掌控 + 全程可追溯
──────────────────────────────────
Git-like版本控制，任意时刻可回滚
```

### 2.4 核心差异化价值

```
┌─────────────────────────────────────────────────────────────────┐
│                      OpenLife 核心差异化                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. 【数据主权绝对化】                                            │
│     人生模型完全本地存储，加密可控，可导出迁移，可继承             │
│                                                                  │
│  2. 【价值观驱动】                                                │
│     所有建议必须通过人生模型过滤器，确保符合用户核心价值观         │
│                                                                  │
│  3. 【终身进化】                                                  │
│     微/周期/深度三层进化，支持人生大事驱动的版本跃迁               │
│                                                                  │
│  4. 【混合架构】                                                  │
│     本地处理隐私+价值观，云端处理复杂推理，最佳平衡                │
│                                                                  │
│  5. 【可解释记忆】                                                │
│     人生模型是结构化的、可读的、可回溯的                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 市场分析

### 3.1 市场规模

#### 3.1.1 基础市场数据（2024-2025年）

| 市场领域 | 2024/2025规模 | 2030/2033规模 | CAGR |
|---------|--------------|--------------|------|
| 个人AI助手市场 | $3.40B | $19.63B | 41.9% |
| AI助手整体市场 | $16.29B | $73.80B | 18.8% |
| 开源AI市场 | $13.43B | $42.82B | 15.6% |
| PKM软件市场 | $1.80B | $4.90B | 11.8% |
| 智能虚拟助手市场 | $14.00B | $288.65B | 31.8% |

#### 3.1.2 OpenLife可触达市场（SAM）

OpenLife的目标市场是上述市场的**交集区域**：
- 隐私优先的本地AI用户
- 个人知识管理（PKM）需求者
- 注重数据主权的知识工作者
- 开源技术采纳者

**市场规模估算（假设7%市场渗透率）**：
- **2025年**: ~$360M（3.6亿美元）
- **2030年**: ~$1.59B（15.9亿美元）
- **复合年增长率**: ~35%

#### 3.1.3 用户规模估算

| 指标 | 数值 | 说明 |
|------|------|------|
| 全球知识工作者 | ~10亿 | 远程/混合办公占比52% |
| 隐私敏感用户占比 | ~15% | 关注数据主权和隐私保护 |
| OpenLife目标用户（TAM） | ~1500万 | 25-45岁知识工作者 |
| 可触达用户（SAM） | ~150万 | 技术采纳意愿较高的早期用户 |

### 3.2 目标用户画像

#### 3.2.1 核心用户画像

**主要画像：隐私优先的知识工作者**
- **年龄段**: 25-45岁
- **职业**: 软件工程师、产品经理、研究员、咨询师、创作者
- **收入水平**: 中高收入（年收入$50K-$150K）
- **技术素养**: 中高，愿意尝试新工具
- **价值观**: 重视隐私、数据主权、个人成长

#### 3.2.2 细分画像

| 画像类型 | 特征描述 | 占比 |
|---------|---------|------|
| **技术先锋** | 开源社区贡献者，关注AI伦理和隐私，愿意投入时间搭建个人系统 | ~30% |
| **效率追求者** | 使用Notion/Obsidian等PKM工具，追求个人成长和工作效率 | ~40% |
| **隐私敏感者** | 因职业原因需要严格数据控制，对云端AI持谨慎态度 | ~30% |

#### 3.2.3 用户需求分析

| 需求层次 | 具体需求 | OpenLife解决方案 |
|---------|---------|-----------------|
| 基础需求 | 数据隐私保护 | 本地存储+加密可控 |
| 功能需求 | 个性化AI助手 | 人生模型+价值观过滤 |
| 情感需求 | 被理解和成长 | 终身进化+意义对齐 |
| 自我实现 | 数据主权+传承 | 可导出迁移+可继承 |

### 3.3 竞争分析

#### 3.3.1 主要竞品详细对比

| 维度 | ChatGPT | Claude | OpenClaw | ZeroClaw | **OpenLife（定位）** |
|------|---------|--------|----------|----------|---------------------|
| **核心资产** | 聊天记录 | 对话历史 | 技能+偏好 | 工具配置 | **人生模型（四维）** |
| **数据存储** | 云端 | 云端 | 本地 | 本地 | **本地加密+可继承** |
| **隐私保护** | ❌ 上传原始数据 | ❌ 上传原始数据 | ✅ 本地处理 | ✅ 本地处理 | **✅ 脱敏上云** |
| **长期视角** | 会话级 | 会话级 | 项目级 | 任务级 | **终身级** |
| **用户主权** | 平台所有 | 平台所有 | 用户所有 | 用户所有 | **用户完全所有+继承** |
| **价值观对齐** | ❌ 通用对齐 | ❌ 通用对齐 | ❌ 无 | ❌ 无 | **✅ 核心价值观过滤** |
| **进化能力** | 模型更新 | 模型更新 | 技能学习 | 工具扩展 | **三层进化+跃迁** |
| **可解释性** | ❌ 黑盒 | ❌ 黑盒 | ⚠️ 部分 | ⚠️ 部分 | **✅ 结构化可读** |
| **开源** | ❌ | ❌ | ✅ | ✅ | **✅** |
| **商业模式** | 订阅 | 订阅 | 开源+服务 | 开源 | **开源+云服务+企业版** |

#### 3.3.2 差异化优势量化

| 优势维度 | 量化指标 | 竞品对比 |
|---------|---------|---------|
| 数据主权 | 100%本地存储+用户完全控制 | 传统AI: 0% |
| 隐私保护 | 原始数据零上云，脱敏率>95% | 传统AI: 100%上云 |
| 长期记忆 | 终身级（数十年） | 竞品: 会话/项目级 |
| 价值观对齐 | 个性化过滤器，符合度>90% | 竞品: 通用对齐 |
| 可继承性 | 支持数字遗产传承 | 竞品: 无 |
| 可解释性 | 结构化人生模型，100%可读 | 竞品: 黑盒/部分可读 |

#### 3.3.3 竞争壁垒分析

| 壁垒类型 | 壁垒描述 | 建立难度 |
|---------|---------|---------|
| **技术壁垒** | 人生模型架构+价值观引擎+进化系统 | ⭐⭐⭐⭐ 高 |
| **数据壁垒** | 用户人生模型积累，迁移成本高 | ⭐⭐⭐⭐⭐ 极高 |
| **社区壁垒** | 开源生态+开发者社区 | ⭐⭐⭐ 中 |
| **品牌壁垒** | "终身成长合伙人"定位 | ⭐⭐⭐ 中 |
| **网络效应** | 插件市场+生态扩展 | ⭐⭐⭐⭐ 高 |

### 3.4 市场趋势和机会

#### 3.4.1 宏观趋势

**趋势1：隐私意识觉醒**
- 2024年后，用户对AI数据隐私的关注度显著提升
- GDPR、数据主权法规推动本地部署需求
- 企业数据泄露事件频发，个人用户警觉性提高

**趋势2：本地AI技术成熟**
- Llama、Mistral等开源模型性能接近闭源模型
- 消费级硬件可运行7B-13B参数模型
- Ollama、LM Studio等本地部署工具生态完善

**趋势3：开源生态繁荣**
- 2025年超过63%的企业尝试开源权重LLM
- 开源AI市场CAGR达15.6%
- 开发者社区对开源个人AI代理框架需求强烈

**趋势4：PKM与AI融合**
- PKM软件市场CAGR 11.8%
- "第二大脑"概念普及（50万+书籍销量）
- AI增强PKM工具提升47%信息检索效率

#### 3.4.2 市场机会

| 机会领域 | 机会描述 | 时间窗口 |
|---------|---------|---------|
| 隐私优先AI | 本地部署需求快速增长 | 2025-2027 |
| 个人AI代理 | 从工具向Agent演进 | 2025-2028 |
| 终身记忆AI | 长期上下文和记忆管理 | 2026-2030 |
| 开源商业化 | 开源核心+增值服务的模式验证 | 2025-2027 |

---

## 4. 功能需求

### 4.1 人生模型构建系统

#### 4.1.1 四维结构管理

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| LM-001 | 身份层管理 | 支持用户定义价值观、个人使命、人生角色 | 1. 可创建/编辑/删除身份条目 2. 支持价值观排序 3. 角色可分配权重 | P0 | - |
| LM-002 | 目标层管理 | 支持长中短期目标的层级定义与关联 | 1. 支持三级目标嵌套 2. 目标间可建立依赖关系 3. 可设置截止日期和里程碑 | P0 | LM-001 |
| LM-003 | 能力层管理 | 构建个人技能图谱，支持技能评估 | 1. 可添加技能节点 2. 支持熟练度评分(1-10) 3. 技能可分类(硬技能/软技能) | P0 | LM-001 |
| LM-004 | 状态层管理 | 追踪身心健康等实时状态指标 | 1. 支持多维度状态录入 2. 可设置状态预警阈值 3. 历史趋势可视化 | P1 | LM-001 |
| LM-005 | 四维关联引擎 | 建立四层之间的动态关联关系 | 1. 身份-目标自动对齐检查 2. 目标-能力差距分析 3. 状态影响目标达成度计算 | P1 | LM-001, LM-002, LM-003, LM-004 |

#### 4.1.2 构建模式系统

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| LM-006 | 深度构建模式 | 4次50分钟深度对话完成完整模型构建 | 1. 分4个会话引导 2. 每次会话有明确主题 3. 支持会话间状态保存 | P1 | LM-001 |
| LM-007 | 快速构建模式 | 60分钟一次性完成基础模型构建 | 1. 单次会话完成 2. 提供精简问题集 3. 生成最小可用模型 | P0 | LM-001 |
| LM-008 | 渐进构建模式 | 边使用边完善模型的增量构建 | 1. 使用时智能提示补充信息 2. 支持随时中断和恢复 3. 自动识别信息缺口 | P0 | LM-001 |
| LM-009 | 构建进度追踪 | 可视化展示模型构建完成度 | 1. 四维进度百分比 2. 构建模式进度保存 3. 完成度预警提示 | P1 | LM-006, LM-007, LM-008 |

#### 4.1.3 苏格拉底式对话引擎

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| LM-010 | 峰值体验挖掘 | 通过对话引导用户回忆和提炼峰值体验 | 1. 开放式问题引导 2. 自动提取价值关键词 3. 生成体验时间线 | P1 | LM-006 |
| LM-011 | 价值排序算法 | 帮助用户对价值观进行优先级排序 | 1. 成对比较交互 2. 排序一致性校验 3. 生成排序结果报告 | P1 | LM-010 |
| LM-012 | 底线测试机制 | 通过假设场景测试价值观底线 | 1. 预设测试场景库 2. 动态生成测试问题 3. 底线边界识别 | P2 | LM-011 |
| LM-013 | 对话状态管理 | 维护多轮对话的上下文和状态 | 1. 支持50+轮对话记忆 2. 话题切换自动恢复 3. 对话摘要生成 | P1 | LM-006 |

### 4.2 双轴分层架构

#### 4.2.1 纵向处理层

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| ARCH-001 | Layer 1 反应层 | <50ms意图分类和快速响应 | 1. 响应延迟<50ms 2. 意图分类准确率>95% 3. 支持20+意图类别 | P0 | - |
| ARCH-002 | Layer 2 战术层 | 500ms-2s主力处理层 | 1. 响应延迟500ms-2s 2. 支持复杂查询处理 3. 上下文理解准确率>90% | P0 | ARCH-001 |
| ARCH-003 | Layer 3 战略层 | 2-5s深度推理层 | 1. 响应延迟2-5s 2. 支持多步推理 3. 生成结构化建议 | P0 | ARCH-002 |
| ARCH-004 | 层间路由机制 | 智能决策请求应该路由到哪一层 | 1. 基于意图复杂度自动路由 2. 支持层间降级 3. 路由决策<10ms | P0 | ARCH-001, ARCH-002, ARCH-003 |
| ARCH-005 | 层间状态传递 | 确保上下文在各层间正确传递 | 1. 状态序列化/反序列化 2. 上下文无损传递 3. 状态压缩率>80% | P1 | ARCH-004 |

#### 4.2.2 横向记忆层

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| ARCH-006 | Tier 0 神经缓存 | 最高速访问的临时缓存 | 1. 访问延迟<1ms 2. 容量限制100条 3. LRU淘汰策略 | P0 | - |
| ARCH-007 | Tier 1 热记忆 | 核心身份信息常驻内存 | 1. 访问延迟<10ms 2. 自动加载核心身份 3. 更新同步机制 | P0 | ARCH-006 |
| ARCH-008 | Tier 2 温记忆 | 完整人生模型存储 | 1. 访问延迟<100ms 2. 支持完整模型查询 3. 增量更新机制 | P0 | ARCH-007 |
| ARCH-009 | Tier 3 检索记忆 | 向量数据库存储语义记忆 | 1. 支持向量相似度搜索 2. Top-K召回准确率>85% 3. 支持混合检索 | P1 | ARCH-008 |
| ARCH-010 | Tier 4 深度归档 | 长期历史数据归档存储 | 1. 压缩存储节省空间 2. 支持按需恢复 3. 归档策略可配置 | P2 | ARCH-009 |
| ARCH-011 | 记忆晋升/降级 | 根据访问频率自动调整记忆层级 | 1. 访问计数统计 2. 自动晋升/降级触发 3. 晋升延迟<1s | P1 | ARCH-006-010 |

### 4.3 持续进化系统

#### 4.3.1 进化机制

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| EVOL-001 | 微进化引擎 | 7天滑动窗口自动权重微调 | 1. 自动收集7天数据 2. 权重调整范围±0.01-0.03 3. 调整后一致性校验 | P1 | LM-001 |
| EVOL-002 | 周期校准机制 | 每周/每月轻量用户确认 | 1. 生成校准报告 2. 用户确认时间5-15分钟 3. 支持批量确认/修改 | P1 | EVOL-001 |
| EVOL-003 | 深度进化触发 | 人生大事触发全面重构 | 1. 大事事件检测 2. 重构流程引导 3. 重构时间60-120分钟 | P2 | EVOL-002 |
| EVOL-004 | 进化回滚机制 | 支持进化结果的撤销和回退 | 1. 保留进化前快照 2. 一键回滚 3. 回滚后状态一致性检查 | P1 | EVOL-001 |

#### 4.3.2 多源融合系统

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| EVOL-005 | 显式反馈收集 | 用户主动提供的反馈(权重0.5) | 1. 反馈入口易访问 2. 支持结构化反馈 3. 反馈影响权重0.5 | P0 | - |
| EVOL-006 | 行为数据收集 | 自动收集用户行为数据(权重0.3) | 1. 行为日志记录 2. 隐私合规处理 3. 行为影响权重0.3 | P0 | EVOL-005 |
| EVOL-007 | 对话推断引擎 | 从对话中推断用户意图变化(权重0.2) | 1. 对话语义分析 2. 意图变化检测 3. 推断影响权重0.2 | P1 | EVOL-005 |
| EVOL-008 | 融合权重计算 | 综合多源数据计算最终权重 | 1. 加权求和公式 2. 权重归一化处理 3. 冲突解决策略 | P1 | EVOL-005, EVOL-006, EVOL-007 |

### 4.4 Hermes集成系统

#### 4.4.1 三层决策框架

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| HERM-001 | 意义层决策 | OpenLife负责意义和价值判断 | 1. 识别意义相关问题 2. 调用人生模型进行判断 3. 输出价值评估结果 | P0 | LM-001 |
| HERM-002 | 策略层协同 | OpenLife与Hermes协同策略制定 | 1. 策略请求分发 2. 协同决策协议 3. 策略一致性保证 | P0 | HERM-001 |
| HERM-003 | 执行层委托 | 执行层任务委托给Hermes | 1. 任务分解和委托 2. 执行结果回调 3. 执行状态同步 | P0 | HERM-002 |
| HERM-004 | 层间通信协议 | 定义三层间的通信标准 | 1. 消息格式标准化 2. 超时重试机制 3. 错误处理规范 | P0 | HERM-001, HERM-002, HERM-003 |

#### 4.4.2 集成组件

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| HERM-005 | 统一意图路由器 | 统一接收和分发用户意图 | 1. 意图解析和分类 2. 智能路由决策 3. 路由日志记录 | P0 | ARCH-004 |
| HERM-006 | 冲突仲裁器 | 解决OpenLife与Hermes的决策冲突 | 1. 冲突检测机制 2. 仲裁规则引擎 3. 冲突解决记录 | P1 | HERM-005 |
| HERM-007 | 技能引擎协同 | 人生模型与技能引擎协同进化 | 1. 技能需求同步 2. 学习路径推荐 3. 技能达成追踪 | P1 | LM-003, HERM-003 |

### 4.6 外部生态集成系统（MCP/A2A 双协议架构）

#### 4.6.1 设计背景与目标

2026年AI Agent生态已被MCP（Model Context Protocol）和A2A（Agent-to-Agent Protocol）主导。为避免OpenLife成为生态孤岛，必须原生支持双协议，实现与Claude Desktop、Cursor、WindSurf等主流工具的无缝集成。

**核心设计原则**：
- MCP作为**工具调用的一等公民协议**：直接复用生态中数千个现成工具
- A2A作为**跨Agent协作协议**：让OpenLife既能调用外部Agent，也能被外部Agent调用
- 自研WASM工具作为**补充**：针对高度隐私敏感或性能关键的场景

#### 4.6.2 MCP协议适配层

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| MCP-001 | MCP客户端核心 | 实现MCP协议客户端，支持Stdio/SSE双传输模式 | 1. 符合MCP 2024-11-05协议规范 2. 支持工具发现、调用、结果返回完整生命周期 3. 支持并发多工具调用 | P0 | TOOL-001 |
| MCP-002 | MCP工具注册表 | 统一管理MCP Server和本地WASM工具的注册、发现、权限控制 | 1. 统一的ToolManifest抽象层 2. 权限分级（Read/Write/Execute） 3. 运行时隔离（进程级/WASM沙箱级） | P0 | MCP-001 |
| MCP-003 | 隐私感知MCP网关 | 在MCP调用前进行隐私检测和脱敏 | 1. 自动识别PII并脱敏 2. 高风险操作用户确认 3. 调用日志本地加密存储 | P1 | MCP-002, PRIV-001 |
| MCP-004 | 常用MCP预配置 | 内置常用MCP Server配置模板（文件系统、浏览器、日历） | 1. 提供5+常用MCP一键配置 2. 自动检测依赖并提示安装 3. 配置向导引导 | P1 | MCP-001 |

**MCP架构图**：
```
┌─────────────────────────────────────────────────────────────────┐
│                    MCP适配层架构                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────────┐   │
│   │ MCP Client  │────►│ Privacy     │────►│ Tool Execution  │   │
│   │ (Stdio/SSE) │     │ Gateway     │     │ (Process/WASM)  │   │
│   └─────────────┘     └─────────────┘     └─────────────────┘   │
│          │                   │                      │            │
│          ▼                   ▼                      ▼            │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────────┐   │
│   │Server       │     │PII Detect   │     │Result Sanitize  │   │
│   │Discovery    │     │& Redact     │     │& Life Model Log │   │
│   └─────────────┘     └─────────────┘     └─────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.6.3 A2A协议适配层

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| A2A-001 | A2A客户端实现 | 实现A2A协议客户端，支持OpenLife主动调用外部Agent | 1. 支持Agent Card发现 2. 支持Task/Artifact消息交互 3. 支持流式响应 | P1 | HERM-004 |
| A2A-002 | A2A服务端实现 | 实现A2A协议服务端，支持外部Agent调用OpenLife | 1. 暴露Life Model查询接口（脱敏后） 2. 支持价值观评估服务 3. 支持意义层决策服务 | P1 | A2A-001, HERM-001 |
| A2A-003 | Hermes-A2A桥接 | 将Hermes协议与A2A协议双向桥接 | 1. Hermes消息转A2A Task 2. A2A Artifact转Hermes结果 3. 保持三层决策语义一致性 | P1 | HERM-004, A2A-002 |
| A2A-004 | 联邦Agent网络 | 支持多个OpenLife实例间的安全协作 | 1. 点对点加密通信 2. 选择性共享人生模型片段 3. 群体智慧决策 | P2 | A2A-002 |

**A2A与Hermes集成架构**：
```
┌─────────────────────────────────────────────────────────────────┐
│              A2A/Hermes双协议桥接架构                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   OpenLife Core                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  ┌──────────┐   ┌──────────┐   ┌──────────┐             │   │
│   │  │Meaning   │   │Strategy  │   │Execution │             │   │
│   │  │Layer     │◄─►│Layer     │◄─►│Layer     │             │   │
│   │  └────┬─────┘   └────┬─────┘   └────┬─────┘             │   │
│   └───────┼──────────────┼──────────────┼───────────────────┘   │
│           │              │              │                        │
│           ▼              ▼              ▼                        │
│   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│   │   A2A Client │ │ Hermes Client│ │   MCP Client │            │
│   │  (对外服务)   │ │  (生态协作)   │ │  (工具调用)   │            │
│   └──────────────┘ └──────────────┘ └──────────────┘            │
│           │              │              │                        │
│           ▼              ▼              ▼                        │
│   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│   │ External     │ │ Hermes       │ │ MCP Servers  │            │
│   │ Agents       │ │ Network      │ │ (npx/env)    │            │
│   └──────────────┘ └──────────────┘ └──────────────┘            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.6.4 统一工具抽象层

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| TOOL-001 | 统一ToolManifest | 定义MCP/WASM/原生工具的统一元数据标准 | 1. 包含工具名称/描述/参数Schema/权限要求 2. 支持版本控制 3. 支持来源标注（MCP/WASM/Native） | P0 | - |
| TOOL-002 | 工具生命周期管理 | 实现工具的安装、更新、禁用、卸载 | 1. 自动依赖检测 2. 沙箱隔离保证安全 3. 回滚机制 | P1 | TOOL-001 |
| TOOL-003 | 工具推荐引擎 | 基于人生模型推荐适合的工具 | 1. 分析目标-能力缺口 2. 推荐相关MCP Server 3. 一键安装配置 | P2 | TOOL-002, LM-005 |

**工具优先级策略**：
```
工具选择优先级（从高到低）：
1. 本地WASM工具（最高隐私、最低延迟）
2. MCP Server（生态丰富、易于配置）
3. A2A Agent调用（复杂任务协作）
4. Hermes技能委托（生态内部协作）
```

### 4.5 版本控制系统

| 需求ID | 需求名称 | 详细描述 | 验收标准 | 优先级 | 依赖 |
|--------|----------|----------|----------|--------|------|
| VCS-001 | Git-like版本管理 | 语义化版本号管理人生模型 | 1. MAJOR.MINOR.PATCH格式 2. 版本自动递增规则 3. 版本变更日志 | P1 | LM-001 |
| VCS-002 | 任意时刻回滚 | 支持回滚到任意历史版本 | 1. 版本列表展示 2. 一键回滚操作 3. 回滚前确认提示 | P1 | VCS-001 |
| VCS-003 | 自动快照机制 | 关键节点自动生成快照 | 1. 快照触发条件配置 2. 快照元数据记录 3. 快照存储优化 | P1 | VCS-001 |
| VCS-004 | 深度归档存储 | 长期历史版本归档管理 | 1. 归档压缩算法 2. 归档恢复机制 3. 归档存储成本控制 | P2 | VCS-003 |
| VCS-005 | 版本对比工具 | 可视化对比不同版本差异 | 1. 四维差异展示 2. 变更高亮显示 3. 导出差异报告 | P2 | VCS-001 |

### 4.6 功能优先级汇总

#### 4.6.1 各模块优先级分布

| 模块 | P0数量 | P1数量 | P2数量 | MVP占比 |
|------|--------|--------|--------|---------|
| 人生模型构建系统 | 5 | 6 | 2 | 38% |
| 双轴分层架构 | 7 | 3 | 1 | 64% |
| 持续进化系统 | 2 | 4 | 2 | 25% |
| Hermes集成系统 | 5 | 2 | 0 | 71% |
| 外部生态集成系统 | 3 | 5 | 2 | 27% |
| 版本控制系统 | 0 | 3 | 2 | 0% |
| **总计** | **22** | **23** | **9** | **41%** |

#### 4.6.2 MVP功能清单（22项P0功能）

**人生模型构建系统 (5项)**
- LM-001: 身份层管理
- LM-002: 目标层管理
- LM-003: 能力层管理
- LM-007: 快速构建模式
- LM-008: 渐进构建模式

**双轴分层架构 (7项)**
- ARCH-001: Layer 1 反应层
- ARCH-002: Layer 2 战术层
- ARCH-003: Layer 3 战略层
- ARCH-004: 层间路由机制
- ARCH-006: Tier 0 神经缓存
- ARCH-007: Tier 1 热记忆
- ARCH-008: Tier 2 温记忆

**持续进化系统 (2项)**
- EVOL-005: 显式反馈收集
- EVOL-006: 行为数据收集

**Hermes集成系统 (5项)**
- HERM-001: 意义层决策
- HERM-002: 策略层协同
- HERM-003: 执行层委托
- HERM-004: 层间通信协议
- HERM-005: 统一意图路由器

**外部生态集成系统 (3项)**
- MCP-001: MCP客户端核心
- MCP-002: MCP工具注册表
- TOOL-001: 统一ToolManifest

### 4.7 用户故事

#### 4.7.1 用户故事 1: 职场转型规划

**用户角色**: 张明，32岁，互联网产品经理，考虑转型创业

**背景**: 张明在互联网公司工作8年，积累了产品设计和团队管理经验。近期开始思考人生方向，希望将工作经验转化为创业能力，但不确定自己的核心价值观和创业方向。

**使用场景**:
1. 张明下载OpenLife，选择"深度构建模式"开始构建人生模型
2. 第一次会话(50分钟)：通过苏格拉底式对话，回顾职业峰值体验，提炼出"创造影响力"、"自主决策"、"持续学习"三大核心价值观
3. 第二次会话：定义人生角色——"创新者"、"终身学习者"、"家庭支柱"，并明确各角色权重
4. 第三次会话：设定5年目标(成功创业)、1年目标(验证商业模式)、3个月目标(完成MVP)
5. 第四次会话：评估现有能力，识别"融资能力"、"销售能力"为关键缺口

**预期结果**:
- 完成完整的四维人生模型构建
- 获得清晰的转型路径图
- 系统自动推荐相关学习资源和行动计划
- 每周收到进度校准提醒

**验收标准**:
- [ ] 4次会话完成率>90%
- [ ] 用户满意度评分>4.5/5
- [ ] 模型构建后30天内用户活跃度>70%

#### 4.7.2 用户故事 2: 健康管理追踪

**用户角色**: 李婷，28岁，程序员，久坐工作导致健康问题

**背景**: 李婷长期加班，体检发现多项指标异常。她希望建立健康的生活方式，但缺乏系统性的健康管理方法，经常三天打鱼两天晒网。

**使用场景**:
1. 李婷选择"渐进构建模式"，先快速设置基础信息
2. 在状态层录入当前健康指标(体重、血压、睡眠质量等)
3. 设置健康目标：3个月减重5kg、每天步行8000步、23:30前入睡
4. 每天与OpenLife简短对话，记录当日状态
5. 系统通过微进化自动调整目标难度，发现李婷对"睡眠"最敏感，优先推荐睡眠改善方案
6. 每周收到5分钟的校准对话，确认目标合理性

**预期结果**:
- 建立个人健康基线
- 获得个性化的健康建议
- 形成持续追踪的习惯
- 3个月后健康指标改善

**验收标准**:
- [ ] 日活跃率>60%
- [ ] 周校准完成率>80%
- [ ] 3个月目标达成率>50%

#### 4.7.3 用户故事 3: 技能学习与职业晋升

**用户角色**: 王强，35岁，技术主管，目标是晋升为技术总监

**背景**: 王强技术能力扎实，但缺乏战略思维和团队管理能力。他需要系统性地提升管理能力，同时保持技术敏感度。

**使用场景**:
1. 王强使用"快速构建模式"，60分钟完成基础模型
2. 在能力层创建技能图谱：技术能力(8/10)、管理能力(4/10)、沟通能力(5/10)
3. 设定晋升目标，系统分析目标-能力差距
4. 系统推荐学习路径：团队管理课程→OKR实践→跨部门协作项目
5. 与Hermes集成，自动将学习任务加入日程
6. 每月深度校准，评估技能提升进度

**预期结果**:
- 清晰的技能发展路径
- 与日常工作结合的学习计划
- 可视化的能力提升进度
- 6个月内管理能力提升至7/10

**验收标准**:
- [ ] 技能评估完成率100%
- [ ] 学习路径采纳率>70%
- [ ] 月度校准完成率>90%
- [ ] 6个月技能评分提升>20%



---

## 5. 技术架构

### 5.1 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              OpenLife 系统架构                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │   Tauri     │    │  React UI   │    │  Hermes     │    │  External   │  │
│  │   Runtime   │◄──►│  Frontend   │    │  Connector  │◄──►│   Agents    │  │
│  └──────┬──────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│         │                                                                   │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Core Service Layer                            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │   Intent    │  │   Memory    │  │   Model     │  │   Tool      │ │   │
│  │  │   Router    │  │   Manager   │  │   Manager   │  │   Registry  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      Processing Pipeline                             │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐      │   │
│  │  │  Layer 1: Reflex│──► Layer 2: Tactical│──► Layer 3: Strategic│      │   │
│  │  │  (<50ms)        │  │  (500ms-2s)     │  │  (2-5s)         │      │   │
│  │  │  ONNX+DistilBERT│  │  llama.cpp      │  │  OpenRouter     │      │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      Memory Storage Layer                            │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │Tier 0   │ │Tier 1   │ │Tier 2   │ │Tier 3   │ │Tier 4   │       │   │
│  │  │KV Cache │ │Hot Mem  │ │Warm Mem │ │Vector   │ │Archive  │       │   │
│  │  │(Model)  │ │(RAM)    │ │(SQLite) │ │(Chroma) │ │(YAML)   │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 核心模块定义

#### 5.2.1 模块划分和职责

| 模块名称 | 职责 | 技术栈 | 资源占用 |
|---------|------|--------|---------|
| `intent_router` | 统一意图识别与路由决策 | Rust + ONNX Runtime | <200MB, <50ms |
| `memory_manager` | 五层记忆系统的统一管理 | Rust + SQLite + ChromaDB | 4-8GB RAM |
| `model_manager` | 三层模型调度与负载均衡 | Rust + llama.cpp | 动态分配 |
| `tool_registry` | 工具注册、发现与执行 | Rust + WASM | 按需加载 |
| `sync_manager` | Hermes双向数据同步 | Rust + gRPC | 后台运行 |
| `security_module` | 隐私检测与加密处理 | Rust + Ring | 常驻内存 |

#### 5.2.2 模块详细职责（Rust伪代码）

```rust
// 意图路由器 (intent_router)
pub mod intent_router {
    // 职责：
    // 1. 接收用户输入，进行快速意图分类
    // 2. 隐私敏感内容检测与拦截
    // 3. 价值观关键词过滤
    // 4. 路由决策：选择处理层 (L1/L2/L3)
    // 5. 冲突检测与仲裁请求
    
    pub struct IntentRouter {
        onnx_session: OrtSession,
        value_filter: ValueFilter,
        privacy_detector: PrivacyDetector,
        routing_table: RoutingTable,
    }
}

// 记忆管理器 (memory_manager)
pub mod memory_manager {
    // 职责：
    // 1. 五层记忆系统的统一接口
    // 2. 记忆晋升/降级调度
    // 3. 上下文窗口管理
    // 4. 记忆检索与融合
    // 5. 记忆持久化与归档
    
    pub struct MemoryManager {
        hot_memory: HotMemoryStore,
        warm_memory: WarmMemoryStore,
        vector_store: VectorStore,
        archive_store: ArchiveStore,
        promotion_policy: PromotionPolicy,
    }
}

// 模型管理器 (model_manager)
pub mod model_manager {
    // 职责：
    // 1. 三层模型生命周期管理
    // 2. 模型加载/卸载策略
    // 3. 请求队列管理
    // 4. 性能监控与自适应调整
    // 5. 云端API调用管理
    
    pub struct ModelManager {
        reflex_engine: ReflexEngine,
        tactical_engine: TacticalEngine,
        strategic_client: StrategicClient,
        load_balancer: LoadBalancer,
    }
}

// 工具注册表 (tool_registry)
pub mod tool_registry {
    // 职责：
    // 1. 工具元数据管理
    // 2. 工具执行环境隔离
    // 3. 权限控制与沙箱
    // 4. 工具发现与推荐
    
    pub struct ToolRegistry {
        tools: HashMap<ToolId, ToolMetadata>,
        executor: WasmExecutor,
        permission_manager: PermissionManager,
    }
}

// 同步管理器 (sync_manager)
pub mod sync_manager {
    // 职责：
    // 1. Hermes协议实现
    // 2. 双向数据同步
    // 3. 冲突检测与解决
    // 4. 离线队列管理
    
    pub struct SyncManager {
        hermes_client: HermesClient,
        conflict_resolver: ConflictResolver,
        offline_queue: OfflineQueue,
    }
}
```

### 5.3 双轴分层架构详解

#### 5.3.1 纵向架构：三层能力模型

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    OpenLife 双轴分层架构                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  纵向：能力分层 (处理能力由弱到强)                                        │
│  ─────────────────────────────────                                      │
│                                                                         │
│     Layer 1          Layer 2           Layer 3                          │
│    (反应层)         (战术层)          (战略层)                          │
│        │                │                 │                             │
│        ▼                ▼                 ▼                             │
│    <100ms          500ms-2s           2-5s                              │
│   快速判断          主力处理          深度推理                          │
│   隐私守门          个性化对话        复杂规划                          │
│   本地ONNX          本地LLM 7B        云端100B+                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Layer 1: 反应层 (Reflex Layer)**

```
定位: 所有请求的"守门员"
延迟: < 50ms (P99)，80ms 降级阈值
资源: < 200MB
技术: ONNX Runtime + DistilBERT (30MB)

核心功能:
├── 意图分类 (10ms)
├── 隐私检测 (10ms)
├── 路由决策 (5ms)
└── 价值观关键词拦截

与记忆关系:
└── 不访问完整人生模型，只用内存关键词表
```

**Layer 1 延迟优化专项方案**

针对 <50ms P99 的极端延迟要求，采用"并行化 + 预加载 + 智能降级"的三重优化策略：

```
┌─────────────────────────────────────────────────────────────────┐
│                    Layer 1 并行优化流水线                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   输入 ──► Tokenizer [~3ms]                                     │
│              │                                                   │
│              ▼                                                   │
│        ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐    │
│        │ 意图分类    │  │ 隐私检测    │  │ 价值观过滤      │    │
│        │ ONNX推理    │  │ 规则引擎    │  │ 关键词匹配      │    │
│        │ [~10ms]     │  │ [~8ms]      │  │ [~2ms]          │    │
│        └──────┬──────┘  └──────┬──────┘  └────────┬────────┘    │
│               │                │                   │             │
│               └────────────────┼───────────────────┘             │
│                                ▼                                 │
│                        ┌──────────────┐                         │
│                        │ 结果合并     │                         │
│                        │ [~3ms]       │                         │
│                        └──────┬───────┘                         │
│                               ▼                                  │
│                        ┌──────────────┐                         │
│                        │ 路由决策     │                         │
│                        │ [~2ms]       │                         │
│                        └──────────────┘                         │
│                                                                  │
│   理论总延迟: ~28ms (并行路径取最大值)                              │
│   安全余量后 P99 目标: 40-50ms                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

1. **并行化执行**：将意图分类、隐私检测、价值观过滤从串行改为并行执行。意图分类走 ONNX DistilBERT（~10ms），隐私检测走优化后的规则引擎（~8ms），价值观过滤走内存 HashSet 关键词匹配（~2ms）。三者并发后，该阶段延迟从串行的 ~25ms 降为并行的 ~10ms。

2. **预加载与常驻内存**：
   - ONNX Session 在应用启动时预加载并常驻内存，避免首次推理 warm-up
   - Tokenizer 词汇表预加载到内存
   - 隐私规则引擎编译为正则缓存池
   - Tauri 后台进程配置为 `KeepAlive`，防止被操作系统回收

3. **智能降级策略**：当总延迟超过 50ms 时，启动分级降级：
   - **50-80ms**：ML 隐私检测降级为纯规则检测
   - **80-120ms**：价值观过滤降级为简化版（只检查 Top 3 核心价值观）
   - **>120ms**：整个 Layer 1 跳过，直接路由到 Layer 2，由 Layer 2 的本地 LLM 承担守门员职责

4. **中英文混合优化**：针对中文用户场景，使用基于字符切分的中文Tokenizer（如 BertTokenizerFast 的 `tokenize_chinese_chars=True`），避免子词切分带来的延迟抖动。

5. **延迟监控与自适应**：
   ```rust
   pub struct ReflexLatencyMonitor {
       pub p99_ms: f64,
       pub last_100_samples: VecDeque<Duration>,
       pub degradation_level: DegradationLevel,
   }
   
   pub enum DegradationLevel {
       Normal,      // 全功能运行
       Light,       // ML隐私检测降级
       Medium,      // 价值观过滤降级
       Bypass,      // 直接路由到Layer 2
   }
   ```

**Layer 2: 战术层 (Tactical Layer)**

```
定位: 日常主力，处理70%请求
延迟: 500ms - 2s
资源: 4-8GB RAM
技术: llama.cpp + Qwen2.5-7B-Q4

核心功能:
├── 整合记忆上下文 (Tier 0-3)
├── 基于人生模型的个性化生成
├── 调用工具/记忆检索
└── 必要时请求Layer 3增援

关键设计 - 上下文注入 vs 微调:
─────────────────────────────────
微调的问题:
• 通用能力下降
• 更新困难（需重新训练）
• 多用户成本爆炸
• 黑盒不可解释

上下文注入优势:
• 模型保持通用能力
• 人生模型变化即时生效
• 精确控制注入内容
• 支持版本回滚
```

**Layer 3: 战略层 (Strategic Layer)**

```
定位: 复杂任务的"增援部队"
触发: 仅当Layer 2请求 (~10%)
延迟: 2-5s (含网络往返)
技术: OpenRouter (Claude/GPT)

核心能力:
├── 深度多步推理
├── 创意内容生成
├── 长文本理解
└── 最新知识问答

隐私保护 - 脱敏上云:
─────────────────────
原始: "我是腾讯高级工程师，年薪80万..."
脱敏: "我是[ROLE:current]在[COMPANY:current]，[INCOME:current]..."
云端: 基于脱敏信息生成通用框架
重构: 本地替换占位符，价值观校验
```

#### 5.3.2 横向架构：五层记忆系统

**记忆系统分层演进方案**

为适应从 MVP 到规模化阶段的不同需求，记忆系统采用**抽象层 + 多后端可插拔**的演进架构：

```
┌─────────────────────────────────────────────────────────────────┐
│                  记忆系统抽象层与后端演进                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │              MemoryManager Trait 抽象层                  │   │
│   │   store() / retrieve() / get_context_window() / promote  │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│          ┌───────────────────┼───────────────────┐               │
│          ▼                   ▼                   ▼               │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│   │ Tier 1 Hot  │    │ Tier 2 Warm │    │ Tier 3 Vec  │        │
│   │  (RAM LRU)  │    │  (SQLite)   │    │ (ChromaDB)  │        │
│   │  固定实现   │    │  可插拔后端  │    │  可插拔后端  │        │
│   └─────────────┘    └─────────────┘    └─────────────┘        │
│                           │                   │                  │
│              ┌────────────┴────────┬─────────┘                  │
│              ▼                     ▼                            │
│        ┌──────────┐          ┌──────────┐                      │
│        │ SQLite   │          │ ChromaDB │                      │
│        │ (默认)   │          │ (默认)   │                      │
│        └──────────┘          └──────────┘                      │
│              │                     │                            │
│              ▼                     ▼                            │
│        ┌──────────┐          ┌──────────┐                      │
│        │ RocksDB  │          │ sqlite-vec│                      │
│        │ (高性能) │          │ (极简)   │                      │
│        └──────────┘          └──────────┘                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**分阶段后端策略**：

| 阶段 | Tier 2 Warm | Tier 3 Vector | 理由 |
|------|-------------|---------------|------|
| **MVP (月1-3)** | SQLite | 暂不实现 | 极简部署，验证核心假设 |
| **产品化 (月4-6)** | SQLite | ChromaDB | 零配置向量检索，生态成熟 |
| **规模化 (月7-12)** | SQLite/RocksDB 可选 | ChromaDB/Qdrant 可选 | 用户根据数据规模选择 |

**Tier 2 抽象接口**：
```rust
#[async_trait]
pub trait WarmMemoryBackend: Send + Sync {
    async fn store(&self, memory: &MemoryItem) -> Result<MemoryId, MemoryError>;
    async fn retrieve(&self, query: &str, limit: usize) -> Result<Vec<RetrievedMemory>, MemoryError>;
    async fn get_by_id(&self, id: &MemoryId) -> Result<Option<MemoryItem>, MemoryError>;
    async fn update_access_stats(&self, id: &MemoryId) -> Result<(), MemoryError>;
}

pub struct SqliteWarmBackend { /* ... */ }
pub struct RocksDbWarmBackend { /* ... */ }
```

**Tier 3 抽象接口**：
```rust
#[async_trait]
pub trait VectorMemoryBackend: Send + Sync {
    async fn upsert(&self, id: MemoryId, embedding: Vec<f32>, memory: &MemoryItem) -> Result<(), MemoryError>;
    async fn search(&self, query_embedding: Vec<f32>, limit: usize) -> Result<Vec<RetrievedMemory>, MemoryError>;
    async fn delete(&self, id: &MemoryId) -> Result<(), MemoryError>;
}

pub struct ChromaVectorBackend { /* ... */ }
pub struct SqliteVecBackend { /* ... */ }
pub struct QdrantVectorBackend { /* ... */ }
```

**记忆晋升/降级简化策略**：
为降低工程复杂度，MVP 阶段采用"触发式 + 定时批处理"，而非实时晋升：
- **热记忆 → 温记忆**：应用退出时统一刷盘，或每 5 分钟 checkpoint
- **温记忆 → 热记忆**：访问时按需加载，命中后缓存 30 分钟
- **温记忆 → 向量记忆**：每天晚上 2:00 批处理，为当天新增记忆生成 embedding
- **向量记忆 → 归档**：每月 1 日批处理，压缩 90 天前未访问的记忆

该策略将复杂的跨层一致性挑战转化为可控的批处理任务，避免实时事务的高复杂度。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    五层记忆金字塔与对应存储                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Tier 0: 神经缓存层 (Neural Cache)                                       │
│  ────────────────────────────────                                       │
│  位置: Layer 2模型内部 (KV Cache)                                         │
│  内容: 当前对话的注意力状态                                              │
│  生命周期: 单次对话会话                                                  │
│  速度: 纳秒级访问                                                        │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Tier 1: 热记忆层 (Hot Memory)                                           │
│  ──────────────────────────────                                          │
│  位置: RAM常驻                                                           │
│  内容: 核心身份 (Top 3价值观、当前角色、一句话使命)                        │
│  大小: ~500 tokens                                                       │
│  生命周期: 应用运行期间                                                  │
│  速度: 微秒级访问                                                        │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Tier 2: 温记忆层 (Warm Memory)                                          │
│  ───────────────────────────────                                         │
│  位置: SQLite + 内存缓存                                                 │
│  内容: 完整人生模型 (身份、目标、能力、状态)                               │
│  大小: ~2000 tokens (完整) / ~800 tokens (摘要)                          │
│  生命周期: 持久化存储                                                    │
│  速度: 毫秒级访问                                                        │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Tier 3: 检索记忆层 (Retrievable Memory)                                 │
│  ─────────────────────────────────────                                   │
│  位置: ChromaDB (本地向量数据库)                                          │
│  内容: 历史对话、行为记录、具体事件、提取的洞察                            │
│  生命周期: 持久化，自动归档                                              │
│  速度: 30-100ms (向量检索 + Rerank)                                      │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Tier 4: 深度归档层 (Cold Archive)                                       │
│  ─────────────────────────────────                                       │
│  位置: 本地文件系统 (SSD)                                                │
│  内容: 完整历史版本、详细行为日志、压缩摘要                                │
│  生命周期: 永久存储                                                      │
│  速度: 100ms+ (按需加载)                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 5.3.3 存储架构

```
~/.openlife/
├── life-model/
│   ├── current/
│   │   ├── metadata.yaml       # 版本信息
│   │   ├── identity.yaml       # 身份层
│   │   ├── goals.yaml          # 目标层
│   │   ├── capabilities.yaml   # 能力层
│   │   └── state.yaml          # 状态层
│   │
│   └── snapshots/              # Tier 4: 历史版本
│       ├── v1.0.0_20260115/
│       ├── v1.1.0_20260201/
│       └── v2.0.0_20260410/
│
├── database/
│   └── openlife.db             # SQLite (Tier 2)
│
├── vectors/                    # Tier 3
│   └── chroma.sqlite3
│
└── archives/                   # Tier 4
    └── 2026/
```

### 5.4 技术栈选择

| 层级 | 组件 | 技术方案 | 选型理由 |
|------|------|----------|----------|
| **Layer 1** | 推理引擎 | ONNX Runtime | 跨平台、高性能 |
| | 意图分类 | DistilBERT-Q8 | 30MB，10ms延迟 |
| **Layer 2** | 推理框架 | llama.cpp | 性能最优 |
| | 主力模型 | Qwen2.5-7B-Q4 | 中文最佳 |
| | Embedding | BGE-M3-Q4 | 中文效果最佳 |
| | 向量DB | ChromaDB | 本地SQLite后端 |
| **Layer 3** | 云端API | OpenRouter | 统一接口 |
| **存储** | 结构化 | SQLite | 成熟可靠 |
| | 人生模型 | YAML | 人类可读 |
| | 加密 | AES-256-GCM + Argon2id | 标准安全 |
| **桌面** | 框架 | Tauri 2.0 + React | 轻量跨平台 |

### 5.5 API接口定义

#### 5.5.1 意图路由器接口

```rust
/// 意图路由器 - 核心接口
pub trait IntentRouter {
    /// 快速意图分类
    /// 
    /// # 参数
    /// - `input`: 用户输入文本
    /// - `context`: 当前会话上下文
    /// 
    /// # 返回
    /// - `Ok(IntentClassification)`: 分类结果
    /// - `Err(RouterError)`: 路由错误
    /// 
    /// # 性能要求
    /// - 延迟: < 50ms (P99)
    /// - 内存: < 200MB
    async fn classify_intent(
        &self,
        input: &str,
        context: &SessionContext,
    ) -> Result<IntentClassification, RouterError>;

    /// 隐私检测
    /// 
    /// # 参数
    /// - `input`: 待检测文本
    /// 
    /// # 返回
    /// - `Ok(PrivacyReport)`: 隐私检测结果
    /// - `Err(PrivacyError)`: 检测错误
    async fn detect_privacy(
        &self,
        input: &str,
    ) -> Result<PrivacyReport, PrivacyError>;

    /// 路由决策
    /// 
    /// # 参数
    /// - `intent`: 已分类的意图
    /// - `complexity`: 任务复杂度评估
    /// 
    /// # 返回
    /// - `Ok(ProcessingLayer)`: 目标处理层
    /// - `Err(RoutingError)`: 路由决策错误
    fn decide_route(
        &self,
        intent: &IntentClassification,
        complexity: TaskComplexity,
    ) -> Result<ProcessingLayer, RoutingError>;
}

/// 输入参数
#[derive(Debug, Clone)]
pub struct SessionContext {
    pub session_id: Uuid,
    pub user_id: String,
    pub conversation_history: Vec<Message>,
    pub current_goals: Vec<Goal>,
}

#[derive(Debug, Clone)]
pub struct IntentClassification {
    pub primary_intent: IntentType,
    pub confidence: f32,
    pub sub_intents: Vec<(IntentType, f32)>,
    pub entities: Vec<Entity>,
    pub requires_tool: bool,
    pub privacy_level: PrivacyLevel,
}

/// 输出参数
#[derive(Debug, Clone)]
pub struct PrivacyReport {
    pub has_sensitive_data: bool,
    pub sensitive_types: Vec<SensitiveType>,
    pub redacted_input: Option<String>,
    pub risk_score: f32,  // 0.0 - 1.0
}

/// 错误码定义
#[derive(Debug, Error)]
pub enum RouterError {
    #[error("模型加载失败: {0}")]
    ModelLoadError(String),
    
    #[error("推理超时")]
    InferenceTimeout,
    
    #[error("内存不足")]
    OutOfMemory,
    
    #[error("无效输入: {0}")]
    InvalidInput(String),
    
    #[error("隐私拦截: {0}")]
    PrivacyBlocked(String),
    
    #[error("价值观冲突: {0}")]
    ValueConflict(String),
}
```

#### 5.5.2 记忆管理器接口

```rust
/// 记忆管理器 - 核心接口
#[async_trait]
pub trait MemoryManager {
    /// 存储记忆
    /// 
    /// # 参数
    /// - `memory`: 待存储的记忆项
    /// - `tier`: 目标存储层级 (可选，自动决定)
    /// 
    /// # 返回
    /// - `Ok(MemoryId)`: 记忆ID
    /// - `Err(MemoryError)`: 存储错误
    async fn store(
        &self,
        memory: MemoryItem,
        tier: Option<MemoryTier>,
    ) -> Result<MemoryId, MemoryError>;

    /// 检索记忆
    /// 
    /// # 参数
    /// - `query`: 查询文本
    /// - `context`: 检索上下文
    /// - `limit`: 返回结果数量限制
    /// 
    /// # 返回
    /// - `Ok(Vec<RetrievedMemory>)`: 检索结果（按相关性排序）
    /// - `Err(MemoryError)`: 检索错误
    async fn retrieve(
        &self,
        query: &str,
        context: &RetrievalContext,
        limit: usize,
    ) -> Result<Vec<RetrievedMemory>, MemoryError>;

    /// 获取上下文窗口
    /// 
    /// # 参数
    /// - `max_tokens`: 最大token数
    /// - `priority`: 记忆优先级过滤
    /// 
    /// # 返回
    /// - `Ok(ContextWindow)`: 上下文窗口
    async fn get_context_window(
        &self,
        max_tokens: usize,
        priority: PriorityFilter,
    ) -> Result<ContextWindow, MemoryError>;

    /// 记忆晋升/降级
    /// 
    /// # 参数
    /// - `memory_id`: 记忆ID
    /// - `target_tier`: 目标层级
    async fn promote(
        &self,
        memory_id: MemoryId,
        target_tier: MemoryTier,
    ) -> Result<(), MemoryError>;

    /// 归档记忆
    /// 
    /// # 参数
    /// - `criteria`: 归档条件
    async fn archive(
        &self,
        criteria: ArchiveCriteria,
    ) -> Result<ArchiveReport, MemoryError>;
}

/// 数据结构定义
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MemoryItem {
    pub id: Option<MemoryId>,
    pub content: String,
    pub memory_type: MemoryType,
    pub importance: f32,  // 0.0 - 1.0
    pub timestamp: DateTime<Utc>,
    pub metadata: MemoryMetadata,
    pub embeddings: Option<Vec<f32>>,
}

#[derive(Debug, Clone)]
pub struct RetrievedMemory {
    pub item: MemoryItem,
    pub relevance_score: f32,
    pub source_tier: MemoryTier,
    pub retrieval_method: RetrievalMethod,
}

#[derive(Debug, Clone)]
pub struct ContextWindow {
    pub memories: Vec<MemoryItem>,
    pub total_tokens: usize,
    pub coverage: f32,  // 记忆覆盖率
}

/// 错误码定义
#[derive(Debug, Error)]
pub enum MemoryError {
    #[error("存储空间不足")]
    StorageFull,
    
    #[error("记忆未找到: {0}")]
    NotFound(MemoryId),
    
    #[error("序列化失败: {0}")]
    SerializationError(String),
    
    #[error("向量索引错误: {0}")]
    VectorIndexError(String),
    
    #[error("层级迁移失败: {0}")]
    TierMigrationError(String),
    
    #[error("并发冲突")]
    ConcurrentModification,
}
```

#### 5.5.3 模型管理器接口

```rust
/// 模型管理器 - 核心接口
#[async_trait]
pub trait ModelManager {
    /// 生成文本 (流式)
    /// 
    /// # 参数
    /// - `request`: 生成请求
    /// 
    /// # 返回
    /// - `Ok(TokenStream)`: token流
    /// - `Err(ModelError)`: 生成错误
    async fn generate_stream(
        &self,
        request: GenerationRequest,
    ) -> Result<TokenStream, ModelError>;

    /// 生成文本 (非流式)
    /// 
    /// # 参数
    /// - `request`: 生成请求
    /// - `timeout`: 超时时间
    /// 
    /// # 返回
    /// - `Ok(GenerationResponse)`: 生成结果
    async fn generate(
        &self,
        request: GenerationRequest,
        timeout: Duration,
    ) -> Result<GenerationResponse, ModelError>;

    /// 加载模型
    /// 
    /// # 参数
    /// - `model_id`: 模型标识
    /// - `layer`: 目标层级
    async fn load_model(
        &self,
        model_id: ModelId,
        layer: ProcessingLayer,
    ) -> Result<(), ModelError>;

    /// 卸载模型
    async fn unload_model(
        &self,
        layer: ProcessingLayer,
    ) -> Result<(), ModelError>;

    /// 获取模型状态
    fn get_model_status(&self, layer: ProcessingLayer) -> ModelStatus;
}

/// 请求/响应定义
#[derive(Debug, Clone)]
pub struct GenerationRequest {
    pub prompt: String,
    pub context: Vec<Message>,
    pub max_tokens: usize,
    pub temperature: f32,
    pub top_p: f32,
    pub stop_sequences: Vec<String>,
    pub tools: Vec<ToolDefinition>,
    pub preferred_layer: Option<ProcessingLayer>,
}

#[derive(Debug, Clone)]
pub struct GenerationResponse {
    pub content: String,
    pub usage: TokenUsage,
    pub finish_reason: FinishReason,
    pub tool_calls: Vec<ToolCall>,
    pub layer_used: ProcessingLayer,
    pub latency_ms: u64,
}

#[derive(Debug, Clone)]
pub struct TokenUsage {
    pub prompt_tokens: usize,
    pub completion_tokens: usize,
    pub total_tokens: usize,
}

/// 错误码定义
#[derive(Debug, Error)]
pub enum ModelError {
    #[error("模型未加载")]
    ModelNotLoaded,
    
    #[error("模型加载失败: {0}")]
    LoadError(String),
    
    #[error("生成超时")]
    GenerationTimeout,
    
    #[error("上下文过长")]
    ContextTooLong,
    
    #[error("GPU内存不足")]
    GpuOutOfMemory,
    
    #[error("API调用失败: {0}")]
    ApiError(String),
    
    #[error("内容被过滤")]
    ContentFiltered,
}
```

#### 5.5.4 错误码统一规范

```rust
/// 全局错误码定义
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ErrorCode {
    // 系统级错误 (1xxx)
    SystemInternalError = 1000,
    ServiceUnavailable = 1001,
    Timeout = 1002,
    RateLimited = 1003,
    
    // 输入错误 (2xxx)
    InvalidInput = 2000,
    MissingRequiredField = 2001,
    InvalidFormat = 2002,
    ContentTooLong = 2003,
    
    // 模型错误 (3xxx)
    ModelNotLoaded = 3000,
    ModelLoadFailed = 3001,
    GenerationFailed = 3002,
    ContextExceeded = 3003,
    
    // 记忆错误 (4xxx)
    MemoryNotFound = 4000,
    StorageFull = 4001,
    SerializationFailed = 4002,
    
    // 安全错误 (5xxx)
    PrivacyViolation = 5000,
    ValueConflict = 5001,
    Unauthorized = 5002,
    
    // 同步错误 (6xxx)
    SyncConflict = 6000,
    NetworkError = 6001,
    VersionMismatch = 6002,
}

/// 统一错误响应
#[derive(Debug, Clone, Serialize)]
pub struct ErrorResponse {
    pub code: ErrorCode,
    pub message: String,
    pub details: Option<Value>,
    pub timestamp: DateTime<Utc>,
    pub request_id: Uuid,
}
```

### 5.6 数据模型详细设计

#### 5.6.1 核心数据结构定义

```rust
// ============================================================================
// LifeModel - 用户数字生命核心模型
// ============================================================================

/// 完整的LifeModel结构
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LifeModel {
    pub metadata: Metadata,
    pub identity: Identity,
    pub goals: Goals,
    pub capabilities: Capabilities,
    pub state: State,
    pub relationships: Relationships,
    pub preferences: Preferences,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Metadata {
    pub id: Uuid,
    pub version: String,           // 语义化版本，如 "1.0.0"
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
    pub schema_version: u32,       // 数据格式版本
    pub checksum: String,          // 完整性校验
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Identity {
    pub core_values: Vec<CoreValue>,
    pub mission_statement: String,
    pub role_definition: RoleDefinition,
    pub personality_traits: Vec<PersonalityTrait>,
    pub voice_style: VoiceStyle,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CoreValue {
    pub name: String,
    pub description: String,
    pub priority: u8,              // 1-10，数值越高优先级越高
    pub keywords: Vec<String>,     // 用于快速匹配的关键词
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RoleDefinition {
    pub primary_role: String,
    pub sub_roles: Vec<String>,
    pub responsibilities: Vec<String>,
    pub boundaries: Vec<String>,   // 角色边界，明确不做什么
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PersonalityTrait {
    pub trait_name: String,
    pub intensity: f32,            // 0.0 - 1.0
    pub context: Vec<String>,      // 该特质适用的场景
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct VoiceStyle {
    pub formality_level: FormalityLevel,
    pub tone_descriptors: Vec<String>,
    pub vocabulary_preference: VocabularyPreference,
    pub emoji_usage: EmojiUsage,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum FormalityLevel {
    Casual,
    Neutral,
    Formal,
    Professional,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Goals {
    pub long_term: Vec<Goal>,      // 1-5年目标
    pub medium_term: Vec<Goal>,    // 3-12个月目标
    pub short_term: Vec<Goal>,     // 1-4周目标
    pub daily: Vec<DailyGoal>,     // 当日目标
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Goal {
    pub id: Uuid,
    pub title: String,
    pub description: String,
    pub status: GoalStatus,
    pub priority: u8,
    pub created_at: DateTime<Utc>,
    pub target_date: Option<DateTime<Utc>>,
    pub progress: f32,             // 0.0 - 1.0
    pub milestones: Vec<Milestone>,
    pub related_memories: Vec<Uuid>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum GoalStatus {
    Active,
    Paused,
    Completed,
    Abandoned,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Milestone {
    pub id: Uuid,
    pub description: String,
    pub completed: bool,
    pub completed_at: Option<DateTime<Utc>>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DailyGoal {
    pub id: Uuid,
    pub title: String,
    pub completed: bool,
    pub time_block: Option<TimeBlock>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TimeBlock {
    pub start: NaiveTime,
    pub end: NaiveTime,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Capabilities {
    pub skills: Vec<Skill>,
    pub tools: Vec<ToolCapability>,
    pub knowledge_domains: Vec<KnowledgeDomain>,
    pub learning_progress: Vec<LearningItem>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Skill {
    pub id: Uuid,
    pub name: String,
    pub description: String,
    pub proficiency: ProficiencyLevel,
    pub acquired_at: DateTime<Utc>,
    pub last_used: Option<DateTime<Utc>>,
    pub usage_count: u32,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum ProficiencyLevel {
    Novice,
    Beginner,
    Intermediate,
    Advanced,
    Expert,
}
```

#### 5.6.2 SQLite Schema

```rust
/// SQLite Schema for Warm Memory
pub const WARM_MEMORY_SCHEMA: &str = r#"
CREATE TABLE IF NOT EXISTS memories (
    id TEXT PRIMARY KEY,
    content BLOB NOT NULL,         -- bincode序列化
    content_type TEXT NOT NULL,    -- message/fact/event/reflection
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL,
    importance_score REAL NOT NULL,
    access_count INTEGER DEFAULT 0,
    last_accessed TIMESTAMP,
    tags TEXT,                     -- JSON数组
    privacy_level TEXT NOT NULL,
    embedding_id TEXT,             -- 关联向量ID
    checksum TEXT NOT NULL         -- 完整性校验
);

CREATE INDEX idx_memories_created ON memories(created_at);
CREATE INDEX idx_memories_importance ON memories(importance_score DESC);
CREATE INDEX idx_memories_accessed ON memories(last_accessed);
CREATE INDEX idx_memories_type ON memories(content_type);
CREATE INDEX idx_memories_privacy ON memories(privacy_level);

-- 全文搜索表
CREATE VIRTUAL TABLE IF NOT EXISTS memories_fts USING fts5(
    content,
    content_rowid=id,
    tokenize='porter'
);
"#;
```

### 5.7 技术实现细节

#### 5.7.1 三层模型调度器

```rust
/// 三层模型调度器实现
pub struct TieredModelScheduler {
    reflex_engine: ReflexEngine,
    tactical_engine: TacticalEngine,
    strategic_client: StrategicClient,
    config: SchedulerConfig,
    metrics: Arc<RwLock<SchedulerMetrics>>,
}

#[derive(Debug, Clone)]
pub struct SchedulerConfig {
    pub reflex_timeout_ms: u64,
    pub tactical_timeout_ms: u64,
    pub strategic_timeout_ms: u64,
    pub enable_auto_fallback: bool,
    pub max_queue_size: usize,
}

#[derive(Debug, Default)]
pub struct SchedulerMetrics {
    pub reflex_requests: u64,
    pub tactical_requests: u64,
    pub strategic_requests: u64,
    pub fallback_count: u64,
    pub avg_reflex_latency_ms: f64,
    pub avg_tactical_latency_ms: f64,
    pub avg_strategic_latency_ms: f64,
}

impl TieredModelScheduler {
    /// 智能路由决策
    pub async fn route_request(
        &self,
        request: &GenerationRequest,
    ) -> Result<ProcessingLayer, SchedulerError> {
        // 1. 检查用户指定的偏好
        if let Some(preferred) = request.preferred_layer {
            if self.is_layer_available(preferred).await {
                return Ok(preferred);
            }
        }

        // 2. 基于任务复杂度决策
        let complexity = self.assess_complexity(request);
        
        match complexity {
            TaskComplexity::Simple => {
                // 简单任务优先使用Reflex层
                if self.reflex_engine.is_available() {
                    return Ok(ProcessingLayer::Reflex);
                }
                // 回退到Tactical
                if self.tactical_engine.is_available() {
                    return Ok(ProcessingLayer::Tactical);
                }
            }
            TaskComplexity::Moderate => {
                // 中等复杂度使用Tactical层
                if self.tactical_engine.is_available() {
                    return Ok(ProcessingLayer::Tactical);
                }
            }
            TaskComplexity::Complex => {
                // 复杂任务使用Strategic层
                if self.strategic_client.is_available() {
                    return Ok(ProcessingLayer::Strategic);
                }
                // 回退到Tactical（可能效果不佳但可用）
                if self.tactical_engine.is_available() {
                    return Ok(ProcessingLayer::Tactical);
                }
            }
        }

        Err(SchedulerError::NoAvailableLayer)
    }

    /// 任务复杂度评估
    fn assess_complexity(&self, request: &GenerationRequest) -> TaskComplexity {
        let mut score = 0u32;

        // 基于token数量
        let estimated_tokens = request.prompt.len() / 4; // 粗略估计
        if estimated_tokens > 2000 {
            score += 3;
        } else if estimated_tokens > 500 {
            score += 1;
        }

        // 基于工具调用需求
        if !request.tools.is_empty() {
            score += 2;
        }

        // 基于上下文长度
        if request.context.len() > 10 {
            score += 1;
        }

        // 基于输出长度要求
        if request.max_tokens > 1000 {
            score += 2;
        }

        match score {
            0..=2 => TaskComplexity::Simple,
            3..=5 => TaskComplexity::Moderate,
            _ => TaskComplexity::Complex,
        }
    }
}
```



---

## 6. 实现路线图

> 说明：本章节保留 PRD 的长期路线图，用于描述目标态。
> 针对当前代码基线的实际开发顺序，不再直接按下面的 12 个月规划执行，
> 而以 [plans/openlife_development_plan.md](/Users/fujing/Desktop/偶来福/plans/openlife_development_plan.md) 和
> [plans/openlife_codex_execution_playbook.md](/Users/fujing/Desktop/偶来福/plans/openlife_codex_execution_playbook.md) 为当前主计划。

### 6.1 开发路线图（12个月）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         开发路线图（12个月）                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 1: 核心基础 (月1-3)                                              │
│  ├── Week 1-2: 项目初始化，Rust workspace，Tauri框架                    │
│  ├── Week 3-4: 人生模型数据结构 + SQLite存储 + 加密                     │
│  ├── Week 5-6: Ollama集成 + 本地7B模型部署                              │
│  ├── Week 7-8: 基础意图识别 + 简单对话                                  │
│  └── Week 9-12: API层 + 前端UI + 基础工具集成                           │
│  里程碑: 可运行的本地Agent，能对话、管理基础人生模型                      │
│                                                                         │
│  Phase 2: 混合架构 (月4-6)                                              │
│  ├── Week 13-14: 隐私过滤层实现                                          │
│  ├── Week 15-16: OpenRouter集成 + 云端API调用                           │
│  ├── Week 17-18: 任务路由决策器                                          │
│  ├── Week 19-20: 响应重构引擎                                            │
│  └── Week 21-24: 价值观校验过滤器                                        │
│  里程碑: 完整的本地+云端混合流程，隐私保护到位                            │
│                                                                         │
│  Phase 3: 进化系统 (月7-9)                                              │
│  ├── Week 25-26: 向量数据库 + 语义检索                                   │
│  ├── Week 27-28: 微进化引擎 + 行为偏差检测                              │
│  ├── Week 29-30: 周期校准对话系统                                        │
│  ├── Week 31-32: 方法论自动提取                                         │
│  └── Week 33-36: 版本快照 + 可视化仪表盘                                │
│  里程碑: 系统能自动进化，用户能看到成长轨迹                               │
│                                                                         │
│  Phase 4: 生态完善 (月10-12)                                            │
│  ├── Week 37-40: 工具生态（邮件、日历、浏览器）                          │
│  ├── Week 41-44: 多平台（移动端同步）                                    │
│  ├── Week 45-48: 插件系统 + 社区分享                                     │
│  └── Week 49-52: 文档完善 + 开源发布                                     │
│  里程碑: v1.0发布，完整五大支柱功能                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 关键依赖路径

| 路径编号 | 依赖链 | 说明 |
|----------|--------|------|
| DP-001 | LM-001 -> LM-002 -> LM-005 | 身份层是目标层和关联引擎的基础 |
| DP-002 | ARCH-001 -> ARCH-002 -> ARCH-003 | 纵向三层必须顺序实现 |
| DP-003 | ARCH-006 -> ARCH-007 -> ARCH-008 | 横向记忆层必须顺序实现 |
| DP-004 | HERM-001 -> HERM-002 -> HERM-003 | 三层决策框架必须顺序实现 |
| DP-005 | LM-001 -> HERM-001 | 意义层决策依赖身份层 |
| DP-006 | ARCH-004 -> HERM-005 | 意图路由依赖层间路由 |
| DP-007 | EVOL-005 -> EVOL-006 -> EVOL-008 | 多源融合依赖各数据源 |

### 6.3 无依赖的独立功能

以下功能可以并行开发，无前置依赖：

| 需求ID | 需求名称 | 建议启动阶段 |
|--------|----------|--------------|
| LM-001 | 身份层管理 | Sprint 1 |
| ARCH-001 | Layer 1 反应层 | Sprint 1 |
| ARCH-006 | Tier 0 神经缓存 | Sprint 1 |
| EVOL-005 | 显式反馈收集 | Sprint 2 |
| VCS-001 | 版本管理 | Sprint 3 |

### 6.4 里程碑规划

```
2025 Q1-Q2: 技术验证阶段
  ├─ GitHub Stars: 1,000+
  ├─ 核心框架发布
  └─ 100早期用户

2025 Q3-Q4: 产品市场契合
  ├─ GitHub Stars: 5,000+
  ├─ 人生模型1.0
  └─ 5,000活跃用户

2026 Q1-Q2: 规模化增长
  ├─ GitHub Stars: 20,000+
  ├─ 插件市场上线
  └─ 50,000活跃用户

2026 Q3-Q4: 商业化验证
  ├─ 云服务盈亏平衡
  ├─ 30企业版客户
  └─ 100,000活跃用户
```

---

## 7. 成功指标

### 7.1 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| Layer 1 响应延迟 | <50ms | 压力测试 |
| Layer 2 响应延迟 | 500ms-2s | 压力测试 |
| Layer 3 响应延迟 | 2-5s | 压力测试 |
| 意图分类准确率 | >95% | 测试集评估 |
| 向量检索Top-K准确率 | >85% | 测试集评估 |
| Tier 0 访问延迟 | <1ms | 基准测试 |
| 系统可用性 | >99.9% | 监控统计 |

### 7.2 功能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 模型构建完成率 | >90% | 用户统计 |
| 周校准完成率 | >80% | 用户统计 |
| 用户满意度 | >4.5/5 | 问卷调查 |
| 日活跃率 | >60% | 用户统计 |
| 目标达成率 | >50% | 用户统计 |

### 7.3 技术指标

| 指标 | 目标值（12个月） | 目标值（24个月） | 测量方法 |
|------|-----------------|-----------------|---------|
| GitHub Stars | 5,000+ | 20,000+ | GitHub API |
| 活跃贡献者 | 50+ | 200+ | GitHub统计 |
| 代码质量 | A级 | A+级 | SonarQube |
| 测试覆盖率 | 70% | 85% | CI/CD |
| 文档完整度 | 80% | 95% | 文档审计 |

### 7.4 产品指标

| 指标 | 目标值（12个月） | 目标值（24个月） | 测量方法 |
|------|-----------------|-----------------|---------|
| 活跃用户数 | 5,000 | 50,000 | 产品分析 |
| 用户留存率（7日） | 40% | 50% | 用户行为 |
| 用户留存率（30日） | 25% | 35% | 用户行为 |
| NPS评分 | 30+ | 50+ | 用户调研 |
| 人生模型创建数 | 3,000 | 30,000 | 产品统计 |
| 平均使用时长 | 30分钟/日 | 45分钟/日 | 产品分析 |

### 7.5 生态指标

| 指标 | 目标值（12个月） | 目标值（24个月） | 测量方法 |
|------|-----------------|-----------------|---------|
| 插件数量 | 20+ | 100+ | 插件市场 |
| 插件下载量 | 10,000+ | 100,000+ | 插件统计 |
| 社区成员 | 2,000+ | 15,000+ | Discord/论坛 |
| 技术博客阅读量 | 50,000+ | 500,000+ | 分析工具 |
| 媒体报道 | 10+ | 50+ | 媒体监测 |

### 7.6 商业指标

| 指标 | 目标值（12个月） | 目标值（24个月） | 测量方法 |
|------|-----------------|-----------------|---------|
| 云服务收入 | $10K/月 | $100K/月 | 财务系统 |
| 企业版客户 | 5+ | 30+ | CRM |
| 企业版收入 | $50K/年 | $500K/年 | 财务系统 |
| 付费转化率 | 2% | 5% | 产品分析 |
| ARPU | $20/月 | $30/月 | 财务系统 |

---

## 8. 风险与缓解

### 8.1 技术风险

| 风险 | 风险等级 | 描述 | 缓解策略 |
|------|---------|------|---------|
| 本地模型性能不足 | 🟡 中 | 7B模型能力有限，复杂任务处理困难 | 混合架构，复杂任务脱敏上云 |
| 人生模型架构不成熟 | 🔴 高 | 四维人生模型缺乏验证 | MVP验证，快速迭代 |
| 价值观引擎偏差 | 🟡 中 | 过滤器可能过度限制或失效 | 用户可调参数，持续优化 |
| 长期记忆管理 | 🟡 中 | 记忆膨胀影响性能 | 分层记忆+智能压缩 |
| 数据安全漏洞 | 🔴 高 | 本地加密被绕过 | 安全审计，开源审计 |
| Layer 1 <50ms延迟难以达成 | 🟡 中 | 性能要求极高 | **并行化+预加载+智能降级，P99目标50ms，有80ms降级兜底** |
| 向量检索准确率不足 | 🟡 中 | 语义理解不准确 | 混合检索策略、持续优化 |
| 多源融合权重冲突 | 🟡 中 | 不同数据源冲突 | 建立冲突仲裁规则 |
| MCP/A2A生态兼容失败 | 🟡 中 | 工具层被生态边缘化 | **MCP作为一等公民，A2A作为跨Agent协议，双协议原生支持** |
| 记忆系统跨层一致性 | 🟡 中 | 五级记忆实时同步复杂 | **抽象层+触发式批处理，MVP阶段不做实时事务** |

### 8.1a Layer 1 延迟风险专项分析

| 子风险 | 影响 | 缓解措施 | 目标 |
|--------|------|----------|------|
| ONNX首次加载延迟 | 冷启动时延迟可达200ms+ | 应用启动预加载，Tauri KeepAlive | 消除冷启动 |
| 串行检测累积延迟 | 意图+隐私+价值观串行执行达25ms+ | 三模块并行化执行 | 压缩到10ms |
| 中文Tokenizer抖动 | 中英文混合切分不稳定 | 字符级中文切分，预编译词汇表 | 延迟波动<5ms |
| 极端场景超时 | 长文本/复杂输入超过50ms | 智能降级：ML→规则→简化→Bypass | P99<50ms，最坏<120ms |

### 8.1b 记忆系统风险专项分析

| 子风险 | 影响 | 缓解措施 | 目标 |
|--------|------|----------|------|
| ChromaDB大规模性能下降 | 条目>10万后检索变慢 | 抽象层支持Qdrant/sqlite-vec切换 | 用户自选后端 |
| 跨层实时一致性复杂 | 实现难度大，Bug多 | 触发式批处理替代实时晋升 | 降低90%一致性风险 |
| SQLite写入瓶颈 | 高频记忆写入卡顿 | WAL模式+连接池+批量写入 | 写入延迟<50ms |

### 8.1c 生态兼容性风险专项分析

| 子风险 | 影响 | 缓解措施 | 目标 |
|--------|------|----------|------|
| MCP协议快速演进 | 协议版本更新导致不兼容 | 封装MCP Client适配器，隔离协议细节 | 2周内完成版本升级 |
| A2A生态不成熟 | 可用Agent少，协作价值低 | 先实现Hermes-A2A桥接，再扩展外部 | 优先服务内部生态 |
| MCP Server安全风险 | 第三方Server可能泄露隐私 | 隐私感知网关+沙箱隔离+权限分级 | 默认最小权限原则 |

### 8.2 产品风险

| 风险 | 风险等级 | 描述 | 缓解策略 |
|------|---------|------|---------|
| 用户不愿投入时间构建模型 | 🔴 高 | 构建过程耗时 | 提供渐进模式、快速模式 |
| 苏格拉底对话体验不佳 | 🔴 高 | 对话流程不够自然 | A/B测试、持续优化对话流程 |
| 与Hermes集成复杂度高 | 🟡 中 | 接口定义复杂 | 定义清晰接口、渐进集成 |
| 用户认知不足 | 🟡 中 | 市场对"人生模型"概念陌生 | 市场教育，案例传播 |
| 技术门槛过高 | 🟡 中 | 部署复杂，普通用户难以上手 | 提供托管版，简化安装 |

### 8.3 市场风险

| 风险 | 风险等级 | 描述 | 缓解策略 |
|------|---------|------|---------|
| 大型竞品入局 | 🔴 高 | OpenAI/Google推出本地产品 | 强化差异化，建立社区壁垒 |
| 开源商业化困难 | 🟡 中 | 开源核心难以变现 | 云服务+企业版+插件市场 |
| 用户付费意愿低 | 🟡 中 | 开源用户习惯免费 | 价值导向定价，免费核心 |

### 8.4 合规风险

| 风险 | 风险等级 | 描述 | 缓解策略 |
|------|---------|------|---------|
| 数据保护法规 | 🟡 中 | GDPR、CCPA等合规要求 | 隐私设计，本地优先 |
| AI伦理审查 | 🟡 中 | 价值观引擎可能引发争议 | 透明机制，用户控制 |
| 数字遗产法律 | 🟡 中 | 继承功能法律地位不明确 | **分阶段实现：v1.0先做导出备份，v2.0再做法律合规继承** |
| 出口管制 | 🟡 低 | 加密技术出口限制 | 合规审查 |

### 8.4a 数字遗产分阶段实现方案

鉴于数字遗产涉及复杂的法律、伦理和技术问题，OpenLife 采用**渐进式、可回撤**的实现策略：

#### v1.0 阶段：数据可导出（MVP，月1-3）

**目标**：让用户感觉自己"拥有数据"，建立信任，但暂不涉及真正的遗产转移。

| 功能 | 说明 | 技术实现 |
|------|------|----------|
| 完整导出 | 一键导出整个人生模型为加密ZIP包 | AES-256-GCM加密，用户自定义密码 |
| 部分导出 | 按维度导出（如只导出身份层） | YAML分文件打包 |
| 迁移导入 | 支持在新设备上导入人生模型 | 密码解密+Schema校验+版本兼容处理 |
| 自动备份 | 定期自动备份到用户指定的本地路径 | 增量备份，保留最近7个快照 |

**法律/伦理边界**：
- 明确告知用户：导出文件由用户自行保管，OpenLife 不托管、不恢复
- 用户协议中声明：导出功能仅供个人数据迁移使用

#### v1.5 阶段：信托备份（月7-9）

**目标**：引入可信第三方托管，为真正的继承做技术验证。

| 功能 | 说明 | 技术实现 |
|------|------|----------|
| 加密分片备份 | 人生模型加密后备份到用户自选云盘 | 端到端加密，OpenLife 无密钥 |
| 恢复码机制 | 生成 12 词恢复码，用于灾难恢复 | BIP39 兼容，离线存储 |
| 紧急联系人 | 用户指定 1-2 位紧急联系人 | 仅发送"备份存在"通知，不发送数据 |

**法律/伦理边界**：
- 紧急联系人仅收到"用户有OpenLife数据备份"的提示，无自动获取权
- 数据的实际转移仍需用户生前手动操作

#### v2.0 阶段：法律合规继承（12个月+）

**目标**：在获得明确法律意见和用户充分知情同意的前提下，实现数字遗产继承。

**前置条件**：
1. **法律咨询**：在目标市场（中国、美国、欧盟）获得明确的法律意见
2. **公证/遗嘱对接**：与当地公证处或数字遗嘱服务商建立合作
3. **家属同意机制**：继承触发需多位家属联合确认
4. **伦理审查**：通过内部或第三方的AI伦理审查

| 继承模式 | 说明 | 技术实现 | 法律要求 |
|----------|------|----------|----------|
| **纪念碑模式** | 只读，冻结在人生模型最后状态 | 导出只读镜像，禁止修改 | 遗嘱明确授权 |
| **火炬模式** | 可继承并继续演化 | 密钥分片托管，满足条件后重建完整密钥 | 多重确认+公证 |

**风险控制**：
```
继承触发流程（火炬模式）:
1. 家属提交继承申请 + 死亡证明
2. 公证处/合作服务商验证法律文件
3. OpenLife系统发送通知给用户的预设联系人（冷却期30天）
4. 冷却期内无用户本人取消指令
5. 多位家属联合签名确认
6. 系统重建解密密钥，生成继承者账户
7. 继承者获得人生模型副本，原账户进入只读纪念碑模式
```

**关键技术：密钥分片托管（Shamir's Secret Sharing）**
```rust
pub struct InheritanceKeyVault {
    /// 用户主密钥被拆分为5片
    pub key_shards: Vec<KeyShard>,
    /// 重建需要至少3片
    pub threshold: u8,
    /// 分片分布
    pub shard_distribution: ShardDistribution,
}

pub struct ShardDistribution {
    pub user_device: KeyShard,      // 用户本地设备
    pub trusted_contact_1: KeyShard, // 紧急联系人1
    pub trusted_contact_2: KeyShard, // 紧急联系人2
    pub legal_escrow: KeyShard,      // 法律托管方
    pub company_hsm: KeyShard,       // OpenLife HSM（仅作为最后手段）
}
```

**v2.0 上线标准**：
- [ ] 三个主要司法管辖区法律意见书
- [ ] 至少一家公证/遗嘱服务商合作
- [ ] 完整的继承审计日志
- [ ] 用户协议中独立的"数字遗产条款"
- [ ] 7x24小时伦理投诉通道

### 8.5 风险矩阵

```
影响程度
    高 │  ┌─────────┐  ┌─────────┐
       │  │大型竞品 │  │人生模型 │
       │  │  入局   │  │不成熟  │
       │  └─────────┘  └─────────┘
       │  ┌─────────┐
       │  │本地性能 │
       │  │  不足   │
    中 │  └─────────┘  ┌─────────┐
       │               │用户认知 │
       │               │  不足   │
       │               └─────────┘
    低 │
       └─────────────────────────────
            低          中          高
                    发生概率
```

---

## 9. 前沿兼容性架构补充

### 9.1 MCP/A2A 双协议集成总览

OpenLife 的工具与 Agent 生态采用**"MCP 向内、A2A 向外"**的双协议策略：

```
                    外部 Agent 网络
                           │
                           ▼ A2A
              ┌────────────────────────┐
              │    OpenLife A2A Server │
              │   (暴露价值观评估服务)  │
              └───────────┬────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌────────────┐  ┌────────────┐  ┌────────────┐
   │ Meaning    │  │ Strategy   │  │ Execution  │
   │ Layer      │  │ Layer      │  │ Layer      │
   └──────┬─────┘  └──────┬─────┘  └──────┬─────┘
          │               │               │
          └───────────────┼───────────────┘
                          │
                          ▼ MCP
              ┌────────────────────────┐
              │   MCP Client + Registry │
              │  (调用文件/日历/浏览器等) │
              └────────────────────────┘
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
          Filesystem   Calendar    Browser
          MCP Server   MCP Server  MCP Server
```

### 9.2 工具选择决策树

```
用户请求需要工具调用时：
│
├─ 是否涉及高度隐私敏感数据？
│  ├─ 是 → 优先使用本地WASM工具
│  └─ 否 → 继续判断
│
├─ 是否有现成MCP Server覆盖？
│  ├─ 是 → 使用MCP Client调用
│  └─ 否 → 继续判断
│
├─ 是否需要跨Agent协作？
│  ├─ 是 → 使用A2A调用外部Agent
│  └─ 否 → 使用Hermes内部技能委托
```

### 9.3 开发路线图更新（含MCP/A2A）

对原 6.1 开发路线图的补充调整：

**Phase 1 补充**：
- Week 9-10：实现 MCP Client 核心（Stdio/SSE 双模式）
- Week 11-12：统一 ToolManifest 抽象层，集成 3 个基础 MCP Server

**Phase 2 补充**：
- Week 19-20：隐私感知 MCP 网关 + 权限分级
- Week 21-22：A2A Client 实现，支持调用外部 Agent
- Week 23-24：A2A Server 实现，暴露 OpenLife 价值观评估 API

**Phase 4 补充**：
- Week 41-42：Hermes-A2A 协议桥接完成
- Week 43-44：联邦 Agent 网络 PoC

---

## 附录

### A. 术语表

| 术语 | 定义 |
|------|------|
| **Life Model** | 人生模型，OpenLife的核心数据结构，包含身份、目标、能力、状态四维 |
| **Reality Alignment** | 真实对齐，确保AI模型与用户真实状态一致的机制 |
| **Social Proxy** | 社交代理，代替用户进行社交互动的功能 |
| **Graduated Autonomy** | 分级自主，根据授权级别执行不同操作 |
| **Memorial Mode** | 纪念碑模式，继承模式之一，只读不可修改 |
| **Torch Mode** | 火炬模式，继承模式之一，可演化发展 |
| **MVP** | Minimum Viable Product，最小可行产品 |
| **P0/P1/P2** | 功能优先级，P0为核心功能，P1为重要功能，P2为增值功能 |

### B. 参考项目

- **Hermes Agent**: 技能进化、用户建模、心跳机制
- **OpenClaw**: 工具系统、执行能力、网关架构
- **Life Model Schema**: 身份/目标/能力/状态四层结构
- **MCP Protocol**: Anthropic 提出的模型上下文协议，工具生态标准
- **A2A Protocol**: Google 提出的 Agent-to-Agent 协作协议
- **Mem0/Letta**: Agent 专用记忆抽象层参考实现

### C. 相关文档链接

| 文档名称 | 路径 |
|---------|------|
| 原始PRD | `/mnt/okcomputer/upload/OpenLife_Complete_PRD.md` |
| 技术架构 | `/mnt/okcomputer/upload/OpenLife_Technical_Architecture_PRD.md` |
| 市场分析 | `/mnt/okcomputer/output/openlife_market_analysis_report.md` |
| 产品分析 | `/mnt/okcomputer/output/OpenLife_PRD_Analysis.md` |
| 技术实现 | `/mnt/okcomputer/output/openlife_technical_architecture.md` |

---

*文档版本: v3.0*
*生成日期: 2025年*
*文档状态: 历史参考*
*适用范围: 长期愿景与历史需求参考，不作为当前 Agent 开发顺序依据*

---

**文档结束**
